# MochiDock 进度记录

## 2026-07-22：阶段 3 宠物提醒方向确认

- 完成首轮业务梳理，并将“宠物提醒”确定为阶段 3 的首个小型工具场景候选。
- 用户确认首版采用循环间隔提醒、允许创建多个但同一时间最多启用一个、通过宠物动作与气泡提醒且默认无声音。
- 产品方向已经确认，但尚未创建或安排编号开发任务；当前没有修改产品代码。
- 下一步是整理最小业务流程、异常规则、界面草图与验收标准，再由用户决定是否安排开发。

## 2026-07-22：MD-011 启动

- 用户正式安排 MD-011 独立设置窗口开发，并明确要求测试先行、保留 MD-010、不得自行验收或归档。
- 已读取项目总览、发现、进度、MD-010 任务书与实施计划，并检查 MD-010 当前未提交差异。
- 已建立 MD-011 独立任务书与实施计划；当前进入 RED 测试阶段。

## 2026-07-22：MD-011 开发交付

- TDD RED：窗口/菜单测试首先因缺少 `MochiDockMenuAction`、`openSettings()` 与设置窗口控制器而编译失败；随后最小实现使聚焦 `MochiDockAppDelegateTests` 全部通过。
- 新增单一 `SettingsWindowController` 与专用 `SettingsView`；状态栏菜单由三项枚举直接渲染，只保留隐藏/显示、设置和退出。
- 设置窗口继续通过 `MochiDockAppDelegate` 调用原尺寸、鼠标接近回应和登录项真实状态路径；打开窗口会刷新 `SMAppService` 映射状态。
- 本地化 RED 揭示 `Settings` / `Settings…` 自动符号冲突；改用 `MochiDock Settings` 窗口标题后，英文与简体中文聚焦测试通过。
- 全量有效 `MochiDockTests` 无签名运行输出 `TEST SUCCEEDED`；其中包括 MD-010 接近回应、持久化、登录项、动画与面板生命周期回归。
- 无签名干净 Debug 构建输出 `CLEAN SUCCEEDED` 与 `BUILD SUCCEEDED`；`git diff --check` 通过。未新增依赖、权限、持久化键或工程能力。
- `xcresulttool` 测试数量汇总受 DerivedData 结果包权限限制失败，不影响原始测试成功证据；该工具错误已记录且未重复规避权限。
- MD-011 已置为“待验收”，未验收、归档、提交、推送或启动后续任务。

## 2026-07-22：MD-011 首轮独立复核

- 新鲜复验通过：全部有效 `MochiDockTests` 共 107 项、MD-005/MD-008 素材稳定测试 11/11、无签名 clean build 和 `git diff --check` 均成功。
- 独立代码复核发现设置窗口前置存在一个 Important 缺口：菜单栏应用打开设置时未显式激活应用，仅调用 `showWindow` 与 `makeKeyAndOrderFront`，在其他应用当前活跃时不能可靠满足“设置窗口前置”。
- MD-011 从“待验收”退回“开发中”。修订范围仅为增加合适的 AppKit 应用激活边界和对应测试；其余交付内容保持不变，人工验收继续暂缓。

## 2026-07-20

### MD-005 第二轮运行时闪烁诊断启动

- 已保留当前全部未提交素材、MD-004 归档、首轮 MD-005 实现和第一轮素材稳定修正；Git 分支仍为 `main`，未提交、未回退。
- 静态追踪确认面板和 `NSHostingView` 均未在切图或尺寸选择时重建，且不存在其他 transition、contentTransition、opacity 或 AppKit animate=true 路径。
- 高可信触发机制得到代码结构证据：动态图片资源与几何属性处于同一个 `.animation(value: animationState)` 子树，视觉状态和动画状态在点击、眨眼与恢复时同轮更新；尺寸模型状态又早于透明面板 frame 更新发布。下一步先建立渲染策略和原子尺寸提交的失败契约测试，不先修改产品实现。

### MD-005 第二轮运行时闪烁修正交付

- TDD RED 新增渲染策略契约后，聚焦测试构建退出 65，明确因 `PetRenderPolicy` 不存在而失败；该次无签名、沙箱或宏服务功能外错误。
- 新增 `PetRenderPolicy`：离散资源和尺寸 transaction 显式禁用动画；呼吸与点击几何阶段继续按原 timing 返回 easeInOut 规格，三段眨眼和 idle 不返回几何动画。`PetView` 不再对整条动态图片链使用统一隐式动画。
- 尺寸更新改为 SwiftUI/AppKit 双层禁动画提交：模型与 panel frame 同组更新，`setFrame(display: false, animate: false)` 后布局并单次 `displayIfNeeded()`；panel 明确使用 `NSWindow.AnimationBehavior.none`。快速五档往返测试确认同一 panel、同一 HostingView、最终尺寸和中心稳定。
- 素材稳定测试 5/5 通过；指定全部有效 `MochiDockTests` 最终退出码 0、`TEST SUCCEEDED`；独立无签名干净 Debug 构建退出码 0、`CLEAN SUCCEEDED`、`BUILD SUCCEEDED`，Asset Catalog 编译成功。
- 干净 Debug 应用通过 LaunchServices 启动，进程保持运行；检查后已终止本次 PID 45078，无 MochiDock 产品残留进程。直接执行 bundle 内二进制曾无输出退出 1，不作为产品失败，改用正常 app 启动路径验证。
- 当前工具未采集桌面画面，无法替代 120/240/320 点击、恢复、眨眼和五档尺寸动态透底观察；MD-005 状态恢复为“待验收”，未标记完成或验收。

### MD-005 点击切图闪烁修正交付

- 原始缺陷是点击切换开心帧及恢复待机时出现全图闪烁；复核确认没有重建面板或改变透明窗口，根因仍是三张动作候选独立生成/去背导致主体 RGB 与 Alpha 漂移，点击亮度是额外放大因素。
- TDD RED：新增 `asset/tests/test_md005_frame_stability.py` 后，5 个测试方法产生 22 个预期功能失败：3 张母帧 Alpha、3 张母帧蒙版外像素、15 张尺寸图 Alpha 及 `PetView` 整图亮度均不满足稳定约束；15 对 Catalog/正式源哈希当时已一致。
- 最小 GREEN：新增版本化面部蒙版与 `asset/tools/stabilize_md005_frames.py`，以原待机母版为固定基底，只合成双眼，开心帧额外合成嘴部；最终强制复用待机 Alpha。五档输出以各档既有待机产品图为固定运行时基底，避免 240/320 既有导出 Alpha 与重新缩放结果的微差。
- 原始 `md-005-master-frames-v1` 候选全部保留；稳定 v2 候选单独存放后升格覆盖正式动作母帧，并确定性生成 15 张正式尺寸源与 15 张 Catalog 副本。像素测试全部通过，所有 Catalog 副本与正式源逐对 SHA-256 一致。
- `PetView` 已移除 mood 驱动的 `.brightness(0.025)`；眨眼顺序、频率、点击优先级、压缩/弹起/落下/恢复、呼吸、面板、菜单、偏好与本地化逻辑均未调整。
- 指定全部有效 `MochiDockTests` 在 `CODE_SIGNING_ALLOWED=NO` 下退出码 0；独立干净 Debug 构建退出码 0，输出 `CLEAN SUCCEEDED` 与 `BUILD SUCCEEDED`，Asset Catalog 编译成功。
- 已目视抽查三张 1254 px 稳定母帧，未见面部蒙版接缝或面部以外变化；当前工具未安全完成真实桌面的动态点击、眨眼、尺寸与拖动检查，因此不声称闪烁体验已目视通过。
- MD-005 已恢复为“待验收”，仍需用户检查 120/240/320 点击与恢复闪烁、眨眼边缘、弹起辨识度、Hide/Show、点击/拖动区分和 30 分钟低打扰体验。

### MD-005 开发交付

- TDD RED：先新增四态五尺寸资源映射、bundle 像素/Alpha、固定眨眼顺序、点击弹起、点击打断眨眼、快速连点和 Hide/Show 调度测试。首次沙箱内测试被 Xcode Swift 宏服务阻断，不作为功能 RED；同一无签名命令在非沙箱环境退出 65，明确因 `PetVisualState`、`resourceName(for:)` 与新动画状态不存在而失败。
- 最小 GREEN：在现有轻量状态机和单一可取消调度任务上加入四种视觉状态、低频眨眼与四阶段程序化弹起；`PetView` 按视觉状态选择母帧，窗口、菜单、持久化和本地化结构保持不变。
- 已把半闭眼、全闭眼、开心三组各五档正式预览原样复制到 `Characters/RedPanda/Prone/v0.4/{HalfBlink,FullBlink,Happy}`，未修改源素材或应用副本像素。
- GREEN 测试使用 `CODE_SIGNING_ALLOWED=NO` 和 `-only-testing:MochiDockTests`，退出码 0、`TEST SUCCEEDED`，共 91 个参数化测试实例通过；其中 20 个运行时资源组合均从 bundle 加载并验证像素尺寸与 Alpha。
- 独立无签名干净 Debug 构建退出码 0，同时输出 `CLEAN SUCCEEDED` 与 `BUILD SUCCEEDED`；15 个 Asset Catalog 应用副本与正式预览源逐对 SHA-256 一致，`git diff --check` 通过。
- 实际启动干净产物成功，MochiDock 进程保持运行；为避免残留额外实例，检查后已终止本次启动的进程。全屏截图因可能采集无关桌面敏感内容被安全策略拒绝，未绕过，因此本轮未目视声明眨眼、点击、菜单或尺寸切换通过。
- MD-005 当前为“待验收”，未标记已完成或已验收；30 分钟低打扰体验、实际拖动/点击区分、动作自然度、眨眼与点击的目视效果、尺寸切换及 Hide/Show/Quit 仍交由用户人工确认。
- 用户实际点击时发现开心帧切换闪烁，首轮交付未通过验收，任务退回“开发中”。只读诊断排除面板重建和窗口背景色变化；120 px 待机/开心帧存在 809 个 Alpha 差异像素，且共同不透明区域 3330 个 RGB 像素变化。根因定位为动作素材独立生成和去背造成的全局帧差，亮度变化是放大因素。当前尚未实施修正。

### MD-007 人工验收完成

- 用户检查 MD-007 英文与简体中文本地化结果并确认没有问题。
- 提交前独立复核未发现 Critical、Important 或 Minor 代码问题；重新运行完整 `MochiDockTests` 退出码 0、`TEST SUCCEEDED`，独立干净 Debug 构建退出码 0、`CLEAN SUCCEEDED`、`BUILD SUCCEEDED`。
- 构建产物包含英文和简体中文本地化资源；偏好键、五个尺寸 raw value、点数和资源名保持不变，`git diff --check` 通过。
- MD-007 状态由“开发中”更新为“已完成”，任务书归档至 `docs/tasks/completed/`。
- 未启动 MD-005。

### MD-007 开发启动

- 用户已正式安排 `MD-007`，任务状态更新为“开发中”；不启动 `MD-005`。
- 已核对 Git 基线 `51aa976`：`main` 比 `origin/main` 领先两个本地提交，启动时工作区干净；本任务不提交、不推送。
- 已完整读取项目记录、任务规则、MD-007 任务书、适用 Skills、全部现有产品 Swift 源码、单元测试和 Xcode 工程配置；仓库内未发现 `AGENTS.md`。
- 启动盘点确认英文开发区域已存在，但尚无 String Catalog 或简体中文区域；12 类运行时展示文案仍由源码英文硬编码提供。
- 实施限定为 Apple 原生 String Catalog 与 SwiftUI/Foundation 本地化类型，不新增语言切换器、第三方依赖、第二份面板/尺寸状态或通用字符串工具容器。

### MD-007 开发交付

- TDD RED：Catalog 文件/搜索检查均退出 1；运行时本地化测试首次构建退出 65，明确因 `PetMood.accessibilityValue` 与所需原生本地化接口不存在而失败。受限沙箱内的签名/宏服务失败与一次测试宿主挂起未计作功能 RED。
- 最小 GREEN：新增 `Localizable.xcstrings`，工程 `knownRegions` 只增加 `zh-Hans`；菜单动态标题、尺寸展示和 mood 无障碍值改用 `LocalizedStringResource`，无障碍标签继续由 SwiftUI 原生本地化字符串输入处理。
- 最终 `build-for-testing` 退出 0；全套 45 项、7 个 `MochiDockTests` 退出 0，输出 `TEST EXECUTE SUCCEEDED`，0 失败。
- 全新派生目录执行 `xcodebuild clean build` 退出 0，输出 `BUILD SUCCEEDED`；构建产物声明 `en`、`zh-Hans` 并包含两份 `Localizable.strings`，逐项内容与 Catalog 一致。
- 实际英文与简体中文 Debug 启动的无障碍树分别确认 `MochiDock pet` / `Resting` 和 `MochiDock 宠物` / `休息中`；两次启动前后生产偏好仍为稳定值 `jumbo`。Computer Use 无法访问 MenuBarExtra，菜单、五档勾选与短暂 Happy 状态未完成目视验证。
- 只读代码审查未发现 Critical 或 Important 问题；审查建议收紧缺译回退测试，已改为 `value: nil` 后重新跑完 45 项测试通过。
- `git diff --check` 通过；产品源码最大 172 行、测试最大 280 行，无职责集中风险。`project.pbxproj` 仅新增 `zh-Hans` known region；未出现依赖、签名、Bundle Identifier、deployment target 或 Release 配置变化。
- 受保护契约与 `51aa976` 对比不变：`pet.displaySize`、`small`、`medium`、`large`、`extraLarge`、`jumbo` 和五个 `RedPandaProneV04_*` 资源名均保持原值。
- MD-007 状态保持“开发中”，等待用户验收；未提交、未推送、未启动 MD-005。

### MD-006 人工验收完成

- 用户完成 MD-006 建议人工验证并确认没有问题，包括尺寸菜单与切换、退出重启恢复、启动表现和重复 Hide/Show 场景。
- MD-006 的开发交付、自动验证与人工验收现已全部完成，状态由“开发中”更新为“已完成”。
- 任务书已从 `docs/tasks/active/` 归档至 `docs/tasks/completed/`。
- MD-007 的 MD-006 前置条件已满足，但仍保持“待用户安排”，未自动启动开发。

### MD-007 本地化范围确认

- 用户确认现在为 MochiDock 建立本地化基础，首批语言限定为英文与简体中文。
- 英文作为开发与回退语言；应用使用 String Catalog 并跟随 macOS 系统或应用语言设置，不增加应用内语言切换。
- 首轮范围为菜单、五档尺寸展示名称和无障碍文案；品牌名 `MochiDock` 以及偏好键、尺寸 raw value 和资源名保持不变。
- 德语、繁体中文、日语及其他语言暂不承诺。
- 已建立 `MD-007` 任务书，状态为“待用户安排”；建议先完成 MD-006 人工验收，再实施 MD-007，之后继续 MD-005。
- 本轮只更新项目管理文档，未修改产品代码，未启动 MD-007 或 MD-005。

### MD-006 开发启动

- 用户已正式安排 `MD-006`，任务状态更新为“开发中”；不启动 `MD-005`。
- 开发范围限定为五档角色尺寸的 UserDefaults 保存、启动恢复、无效值回退和可注入隔离存储；不创建 SwiftData 容器、Schema 或领域模型。
- 已检查 Git 基线 `e5db3bf` 与既有工作区修改；启动前仅三份项目记录有本地修改，MD-006 任务书为未跟踪文件，均保留并在原内容上增量更新。
- 已建立逐步实施计划 `docs/superpowers/plans/2026-07-20-md-006-local-persistence-foundation.md`。
- TDD RED 已确认：新增测试后聚焦构建退出码 65，因 `PetPreferencesStoring` 尚不存在而失败，原因与预期缺失功能一致。
- 最小 GREEN 实现新增偏好协议、UserDefaults 适配器、集中键名、同步恢复和立即写入；聚焦偏好、面板和动画测试退出码 0。
- 首次 GREEN 暴露两个默认偏好参数的主线程隔离警告；已移除默认参数并让所有测试显式注入内存存储，第二次聚焦 GREEN 无该 Swift 警告。
- 独立代码审查未发现 Critical 或实现缺陷；提出菜单选择源未直接断言（Important）和生产适配器未测试（Minor），已分别补充 AppDelegate 恢复集成测试和唯一 UserDefaults suite 测试。
- 最终指定 `MochiDockTests` 全套测试退出码 0、`TEST SUCCEEDED`；指定干净 Debug 构建退出码 0、`CLEAN SUCCEEDED`、`BUILD SUCCEEDED`。
- `git diff --check` 通过；产品源码最大 165 行、测试最大 280 行，均低于约 400 行职责复核阈值；工程文件无差异，未引入第三方依赖或发布配置变化。
- 实际运行将 MochiDock 自身偏好设为稳定标识 `jumbo`，干净构建产物首次启动和退出重启后均显示 320 尺寸角色；当前保留该值供人工验收。
- Computer Use 无法访问 MenuBarExtra Picker，故实际菜单点击写入、菜单勾选目视与启动瞬间无跳变仍未验证；自动测试已覆盖对应状态和初始化顺序。
- MD-006 保持“开发中”，等待用户和高级项目经理验收；未提交、未推送、未启动 MD-005。

### 本地持久化方案确认

- Dock 通用占位图标的现场原因为 Xcode 清理/重建后旧进程仍在运行，其原始 App bundle 一度不存在；用户清理缓存并重新运行后 App Icon 已正常显示，无需产品代码修正。
- 用户确认本地持久化分层方案：UserDefaults 负责轻量偏好，SwiftData 负责未来成长、服饰与关系型领域数据，图片本体不入库。
- 已建立 `MD-006` 任务书，最小可验证体验是“用户选择尺寸后，退出并重启仍恢复该尺寸”；任务建议优先于 `MD-005` 实施，但当前尚未安排开发。

## 2026-07-19

### MD-004 开发启动

- 用户已正式安排 `MD-004`，任务状态更新为“开发中”；不启动 `MD-005`。
- 已完整读取根目录项目记录、MD-004/MD-005 任务书与素材规则，并确认 Git 基线为 `cfeea74a6ec84c819f71073b41648e084219bd0c`。
- Git 工作区仅有用户声明的两个未跟踪试验目录：`asset/workbench/` 与 `asset/character/red-panda/master/v0.4-prone-candidate/`；本任务不触碰它们。
- 三张 v0.4 正式预览源图分别为 80×80、120×120、160×160，均带 Alpha。
- 首次源码盘点错误地按仓库根目录猜测测试和 Assets 路径，三个只读查询失败；已从文件清单确认工程实际嵌套在 `MochiDock/` 子目录，后续改用正确路径。
- 实施遵循测试驱动开发：先新增能覆盖资源映射、状态推进、底部锚定与单循环调度的失败测试，确认 RED 后再写最小产品实现。
- 已将逐步实施计划写入 `docs/superpowers/plans/2026-07-19-md-004-animation-playback-foundation.md`；采用单一可取消调度任务、模型负责状态、面板负责启停、视图只负责渲染的职责边界。
- TDD RED 已确认：聚焦测试构建因新的 `PetInteractionModel(scheduler:)`、播放状态和调度协议尚不存在而失败，失败原因与预期缺失行为一致。
- 首次 GREEN 构建暴露 Swift 主线程隔离错误：默认参数表达式不能创建 `@MainActor` 调度器。根因确认后改为无参数生产初始化器与显式测试注入初始化器，未放宽并发隔离。
- 面板生命周期 RED 已确认：在撤下启停接线后，`repeatedShowAndSizeChangesKeepOnePlaybackSchedule` 与 `hideStopsPlaybackAndShowRestartsOneSchedule` 失败，其余原有面板测试通过；随后重新接入 Show/Hide 启停。
- 最终 `MochiDockTests` 全套 40 项通过、0 失败；新测试覆盖实际状态推进、可注入调度、静止间隔、峰值上限、底部锚点、快速点击、Show/Hide 单循环、资源映射与运行时像素/Alpha。
- 独立 `xcodebuild clean build` Debug 构建成功；`git diff --check` 通过；三档应用 PNG 与正式源文件哈希逐对一致。
- 文件职责与行数已检查：产品源码最大 138 行，测试最大 219 行，均低于约 400 行职责复核阈值。
- 实际启动确认默认显示单一 120 档 v0.4 趴姿窗口且无裁切；点击后自动恢复 Resting；通过应用菜单触发 Quit 成功。
- Computer Use 三帧采样无法可靠辨别 2.2% 轻微起伏，因此实际呼吸可感度、底部稳定和纹理无闪烁仍需用户人工体验；状态栏尺寸菜单、Hide/Show 与真实拖动也未完成工具操作验证。

### MD-004 大尺寸验收调整

- 用户确认首轮交付无其他问题，但 160 在 16 英寸 MBP 上偏小，要求新增 240/320 两档；本轮保持 MD-004 “开发中”，未启动 MD-005。
- TDD RED 已确认：尺寸与面板测试因 `PetDisplaySize.extraLarge` 与 `.jumbo` 尚不存在而编译失败，失败原因符合预期。
- 已从已确认的 1254×1254 v0.4 透明母版确定性缩放生成 240/320 PNG，并新增对应 image set、尺寸枚举、资源映射与菜单标题；默认仍为 120。
- 定向 GREEN 已确认：新增两档的资源加载、像素/Alpha、状态更新与面板尺寸测试通过。
- 完整 `MochiDockTests` 测试套件通过、0 失败；干净 Debug 构建成功；`git diff --check` 通过。
- `sips` 独立检查确认新资源分别为 240×240 与 320×320，均保留 Alpha。真实菜单切换观感仍留给用户人工验收。

### MD-004 App Icon 与资源分类收尾

- 用户要求补齐 1024×1024 App Icon，并解决 Asset Catalog 图片平铺、后续难查找与替换的问题；本轮继续归入 MD-004 收尾，未启动 MD-005。
- 使用内置图像生成工具，以 v0.4 趴姿角色为身份与画风参考，生成温暖奶油背景的小熊猫头像 App Icon；不修改正式角色母版。
- 生成工具原始输出为 1254×1254，与用户要求不符；已确定性缩放为真正的 1024×1024 不透明 PNG，再从该标准源图生成 macOS AppIcon 全部尺寸槽位。
- 品牌源图保存于 `asset/brand/app-icon/v0.1/`；Asset Catalog 整理为 `System`与 `Characters/RedPanda/Prone/v0.4` 层级，未启用 namespace，现有资源名与 Swift 映射保持不变。
- 首次干净 Debug 构建已通过，证明目录调整后 AppIcon 与角色资源仍能被 Asset Catalog 编译。
- 最终完整 `MochiDockTests` 测试套件通过、0 失败；最新干净 Debug 构建成功；`git diff --check` 通过。
- 构建产物 `Info.plist` 明确包含 `CFBundleIconName = AppIcon`；`Assets.car` 独立检查确认 16、32、128、256、512 的 1x/2x 图标 rendition 与 1024 最高分辨率均已编译，五档角色资源名与像素也保持正确。

### 阶段 1B 方向确认

- 用户确认将小熊猫的常驻待机姿势从站立改为趴着，认为站立不适合长时间待机。
- 其余基础方向按推荐确认：轻微呼吸或身体起伏、偶尔眨眼、动作间有停顿，以及点击后开心弹起并自动恢复待机。
- 默认显示尺寸继续采用 120 px，并保留 80/120/160 三档切换。
- 用户确认前期只跑最基础功能，想法池内容不进入本轮实现。
- 已将阶段 1B 拆分为趴姿待机素材准备、`MD-004` 动画播放基础和 `MD-005` 待机/点击动作接入。
- 用户进一步确认后续动画素材采用“三张新母帧＋程序化弹起”：母帧提供必要的造型/表情切换，弹起与回落轨迹由程序生成，不制作完整逐帧弹跳序列；三张母帧的具体画面仍需在出图前锁定。
- 三张新母帧的具体画面已获用户确认：半闭眼趴姿、全闭眼趴姿、开心趴姿。眨眼复用半闭眼帧完成往返；点击使用开心趴姿，压缩蓄力、弹起、落下和恢复由程序实现。
- 已使用内置图像生成工具，以 v0.4 绿幕趴姿母版为严格编辑参考生成三张 v1 工作台候选，并完成本地绿幕去除：半闭眼、全闭眼、开心趴姿均为 1254×1254 RGBA。另生成 120 px 眨眼与程序化弹起评审 GIF；当前仅供用户视觉评审，尚未升格为正式母帧、生成五档产品素材或接入 `MD-005`。
- 用户确认 v1 视觉预览暂时没有问题。三张透明候选已原样升格到 `asset/character/red-panda/master/v0.4/`；SHA-256 与对应候选一致。已从每张母帧用 LANCZOS 确定性生成 80/120/160/240/320 五档素材，共 15 张，尺寸、Alpha 和透明四角检查全部通过；尚未复制到 Asset Catalog 或启动 `MD-005`。
- 用户说明 `MD-004` 已完成人工验收，并正式要求在复核后安排 `MD-005`。收口复核首次执行完整测试时，当前终端缺少团队 `RS44S3AN5X` 的 Mac Development 私钥，Xcode 在构建阶段取消测试；尚未进入任何测试用例。下一步以 `CODE_SIGNING_ALLOWED=NO` 重新执行本地完整测试和干净 Debug 构建。
- 无签名 scheme 测试中全部 `MochiDockTests` 逐项通过，但空的 `MochiDockUITests` bundle 因找不到可执行文件导致整体退出码 65；该次不作为成功证据。下一步指定 `-only-testing:MochiDockTests` 重跑全部有效单元测试，并独立执行干净 Debug 构建。
- `MD-004` 收口复核完成：指定全部有效 `MochiDockTests` 的无签名本地测试退出码 0、`TEST SUCCEEDED`；独立无签名干净 Debug 构建退出码 0、`CLEAN SUCCEEDED`、`BUILD SUCCEEDED`。结合用户已完成并再次确认的人工验收，`MD-004` 状态更新为“已完成”并移入 `docs/tasks/completed/`。
- 用户正式安排 `MD-005`；任务状态由“待用户安排”更新为“开发中”。当前范围只包含偶尔眨眼、三张确认母帧接入、程序化开心弹起与恢复待机，不扩展鼠标跟随或更多随机行为。

### v0.4 趴姿静态母版确认

- 用户确认纯本地 workbench v5 的透明边缘结果，同意升格为正式 v0.4 趴姿母版。
- 已将确认文件原样保存到正式 `master/v0.4` 目录；正式主图与 v5 候选 SHA-256 完全一致。
- 已从正式透明母版本地生成 80×80、120×120、160×160 三档 RGBA PNG；尺寸、Alpha 通道和透明四角检查通过，120 px 预览已目视检查。
- 本轮未调用付费图片 API、未重新绘制角色、未修改产品代码。
- 阶段 1B 素材的下一步是制作轻微呼吸、偶尔眨眼和开心弹起后恢复趴姿的动作帧。
- 已生成第一张“呼吸最高点”workbench 候选及 120 px 往返预览，等待用户评审动作幅度与帧间稳定性；本次仍未调用付费图片 API，也未接入产品代码。
- 用户评审判定该生成式候选只有背景闪烁、角色没有可见动作；候选已明确淘汰。已改为验证“同一透明素材、底部锚定、轻微纵向缩放”的确定性呼吸方案，当前只有本地预览，尚未修改产品代码或任务书实施方式。
- 用户确认已经看到确定性预览的起伏，同意先试用该版本。已将 `MD-004` 更新为趴姿接入、底部锚定程序化呼吸与最小动画状态基础；`MD-005` 不再等待呼吸帧，只保留眨眼与点击回应接入。当前仍未修改产品代码。

### MD-002 开发交付与高级项目经理初审

- Codex 开发部门已将小熊猫 v0.3 的 80、120、160 三档透明素材分别接入独立 image set，以正式角色替换临时 SwiftUI 绘图，并加入默认 120、非持久化尺寸 Picker 和克制的点击反馈。
- 高级项目经理读取本地 Handoff，核对 Git 差异、源码、测试、资源目录和工程配置；未发现 Critical 或 Important 问题，未发现 `project.pbxproj`、签名、Bundle Identifier、deployment target 或第三方依赖变化。
- 高级项目经理使用独立派生目录重新执行目标测试，结果 22 通过、0 失败、退出码 0；重新执行干净 Debug 构建，退出码 0；`git diff --check` 通过。
- 三张源图与应用副本分别为 80×80、120×120、160×160，均有 alpha，成对 SHA-256 一致；构建产物包含三个独立 scale 1 rendition。
- 独立代码审查未发现阻塞项；非阻塞观察为面板测试未主动清理窗口，以及点击手势与背景拖动组合仍需人工验证。
- MD-002 当前状态为“待验收”。人工待验收项目包括尺寸菜单与选中标记、点击反馈、实际拖动、Show Pet/Quit、三档遮挡感、透明绿边和 30 分钟稳定性。
- 用户人工确认三档尺寸菜单切换正常，连续运行 30 分钟没有异常。
- 用户发现菜单只有 `Show Pet`、没有 `Hide Pet`；由于无边框窗口也没有关闭按钮，正常用户路径无法先隐藏宠物，因此 `Show Pet` 恢复闭环当前不可验收。
- MD-003 首轮双按钮实现已由开发部门交付并通过 27 个自动测试，但用户在人工查看菜单后要求把 `Show Pet` 与 `Hide Pet` 合并为一个随面板可见性变化的动态菜单项；首轮交付暂不验收，继续在 MD-003 内调整。
- 开发部门随后仅重新验证首轮双按钮实现，并错误报告“无需重复修改”；源码仍有两个独立 Button，Handoff 也没有动态菜单修订章节。该次报告不满足 MD-003 修订要求，任务继续保持开发中。
- 开发部门最终完成动态单菜单修订：固定双按钮已移除，菜单标题实时读取 `NSPanel.isVisible`，切换保持同一面板、尺寸和 mood，并通过 `ObservableObject` 通知 SwiftUI 重新求值。
- 高级项目经理独立复跑 30 个测试，结果 30 通过、0 失败、退出码 0；独立干净 Debug 构建退出码 0；固定双按钮静态搜索为零匹配，`git diff --check` 通过。MD-003 进入人工验收。
- 用户人工验收时曾建议把动态标题简化为固定 `Show/Hide Pet`，并报告当前版本启动后宠物没有自动出现；进一步诊断后保留动态标题，以便菜单直接说明下一次点击的动作。
- 开发部门定位出两个独立根因：面板使用 `.moveToActiveSpace` 时可能停留在其他 Space；原 MenuBarExtra 内容没有以观察对象方式订阅 AppDelegate，导致标题停留在启动前的 `Show Pet`。
- 最小修正将面板行为调整为 `.canJoinAllSpaces`，并由 `MochiDockMenuContent @ObservedObject` 构建菜单。开发部门在单一最新 Debug 实例中完成启动显示、Hide、Show、标题往返和同一宠物恢复的真实闭环。
- 高级项目经理提交前独立复跑 31 个测试，结果 31 通过、0 失败、退出码 0；独立干净 Debug 构建退出码 0，`git diff --check` 通过。MD-003 当前为“待验收”。

### 角色母版资料迁入

- 从今天 13 点左右的历史任务“整理小熊猫桌宠设定”中核对并恢复了完整角色设计记录。
- 将原任务中的角色母版 v0.2、角色设计决定、进度和计划迁入 `docs/design/character/`。
- 当前根目录项目总览、已确认结论和进度记录已加入角色设计入口，没有覆盖原有项目管理内容。
- 已确认方向包括：圆润幼崽、活泼好奇且略冒失、尾巴旋转飞行、柔软略笨拙的喜剧飞行、稍明亮自然红棕毛色和中等大小圆眼。
- 下一步是确定角色轮廓、面部花纹细节与精确色板，并制作第一轮视觉草图。

## 2026-07-18

### 已完成

- 创建 MochiDock 本地 Xcode 项目。
- 建立 Git 管理与根目录 `.gitignore`。
- 确认 `G_MochiDock` ChatGPT 项目未绑定当前本地项目目录。
- 找到相关历史任务并恢复此前关于产品方向、代码规模、AI 开发模式、Handoff 和 PM Skill 的讨论结论。
- 将恢复后的规划写入当前项目：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

### 当前状态

- 项目处于产品与技术探索初期。
- 本地项目现在已经拥有可跨任务持续读取的规划记录。
- `mochidock-pm` 第一版已经创建并通过校验。

### 风险

- 原隐藏项目目录中曾建立的规划文件没有保留在当前磁盘视图，本次内容来自历史任务中的已确认讨论重建，并非逐字文件复制。
- 首个产品阶段若过早引入复杂 AI 功能，可能扩大范围并延迟可体验版本。

### 阻塞

- 当前无开发阻塞。

### 下一步

1. 规划桌面宠物阶段 1 的最小交互原型。
2. 明确阶段 1 的验收标准。

### Skill 设计续接

- 已核对当前项目中的持久化记录，确认关键讨论内容已经恢复，原始对话文件未迁入但不构成继续设计的阻塞。
- 已创建 `sources/ai-development-mode-summary.md`，保存 AI 开发连续性、Handoff 和典型使用例子。
- 已创建 `skill-blueprints/mochidock-pm.md`，定义定位、触发条件、边界、核心工作流、资源和验收标准。
- 此时尚未初始化正式 Skill；后续已由用户确认保存位置并完成创建。

### `mochidock-pm` 第一版完成

- 用户确认保存到 `/Users/tao/Projects/Working/skills/mochidock-pm/`。
- 已创建 `SKILL.md`、`agents/openai.yaml`、`references/project-rules.md` 和 `assets/handoff-template.md`。
- 已建立用户级发现链接 `~/.agents/skills/mochidock-pm`。
- 官方快速校验结果为 `Skill is valid!`。
- 已完成只读前向测试：面对 AI 聊天、喝水提醒和文件整理同时要求立即实现的请求，Skill 正确保留阶段 1、完成想法分组、说明风险并等待用户确认。

### 阶段 1A 项目整理

- 已确认下一步从 Skill 优化切换到 MochiDock 项目推进。
- 已核对 Xcode 模板源码、测试 target、工程同步分组和本机 Xcode 版本。
- 已确定阶段 1A 最小闭环：透明无边框显示、拖动、点击反馈、菜单栏恢复与退出。
- 已明确非目标：正式素材、数据保存、复杂动画、工具能力、AI、网络和第三方依赖。
- 已建立详细实施计划：`docs/superpowers/plans/2026-07-18-stage-1a-minimum-desktop-pet.md`。
- 当前尚未修改应用代码；下一步是按测试先行方式执行阶段 1A 计划。

### 协作职责调整

- 用户明确：当前 Agent 的身份是高级项目经理，不承担产品代码开发。
- Codex 开发部门负责代码实现和技术自测。
- 后续遇到开发节点时，高级项目经理 Agent 将明确提示用户安排 Codex，并提供可直接转交的开发任务说明。
- 是否启动开发任务由用户决定和安排；高级项目经理 Agent 负责后续只读核对、验收和范围管理。

### 任务文档结构建立

- 已将 `task_plan.md` 精简为项目路线、当前范围和任务索引，不再承载单个开发任务的详细步骤。
- 已建立 `docs/tasks/active/` 与 `docs/tasks/completed/`，并定义任务编号、状态和归档规则。
- 已建立按需使用的 `docs/decisions/` 和 `docs/handoffs/` 说明，避免所有信息堆叠到总计划。
- 已创建首份正式开发任务书 `MD-001`，状态为“待用户安排”。
- 本轮只修改项目管理文档，没有修改产品代码。

### MD-001 开发提交与高级项目经理初审

- Codex 开发部门已完成阶段 1A 产品代码、测试和开发验证，未创建 Git commit。
- 用户已实际启动应用，确认只出现一个透明无边框宠物，并确认第一次点击可从休息变为开心；第二次点击恢复休息仍待人工确认。
- 高级项目经理对任务书、Git 差异、源码、测试和工程配置进行了只读复核：未发现 Critical 或 Important 级别问题，也未发现第三方依赖、SwiftData 残留、Xcode 工程配置或签名设置变更。
- 高级项目经理使用独立派生目录重新执行 6 个目标测试，结果 6 通过、0 失败；重新执行干净 Debug 构建，退出码为 0；`git diff --check` 通过。
- 独立代码审查认为可以进入人工验收，并记录两个非阻塞测试质量项：面板测试未主动清理真实窗口；尚未单独覆盖 `hidePet()` 后通过 `showPet()` 恢复。
- MD-001 当前状态更新为“待验收”。尚未验证：实际拖动、菜单栏恢复与退出、切换应用后的低打扰表现、多次恢复，以及连续 30 分钟稳定性。
- 已创建任务现场交接记录：`docs/handoffs/2026-07-18-MD-001-stage-1a.md`。
- MD-001 产品代码与测试已独立提交为 `cea1d55 feat: add minimum desktop pet`；项目管理文档保留为单独提交边界。

### MD-005 闪烁修正独立复核

- 高级项目经理已独立检查稳定素材流程、像素约束、产品代码差异和变更边界，未发现阻止验收的问题。
- 新鲜运行素材稳定测试 5/5 通过，全部有效 `MochiDockTests` 通过，独立干净 Debug 构建通过，`git diff --check` 通过。
- 三张 stable-v2 母帧静态目视未见明显局部接缝或外轮廓漂移。
- `MD-005` 保持“待验收”；下一步由用户在真实桌面复验点击切换、落下恢复、眨眼边缘及 Hide/Show 等动态体验。

### MD-005 第二轮人工验收反馈

- 用户确认稳定素材修正后角色边缘已稳定，但点击切图、眨眼和尺寸切换仍会短暂透出宠物后方内容，因此 MD-005 再次退回“开发中”。
- 落下恢复时是否存在第二次闪烁目前无法确认，继续列为待复验项。
- 下一轮开发只处理离散资源切换的动画隔离与透明面板尺寸重绘，不重新制作素材，不改变已确认的动画节奏和产品范围。

### MD-005 第二轮修正复验

- 第二轮运行时修正交付后，高级项目经理独立复核代码边界并重新运行素材测试、全部有效应用测试和干净 Debug 构建，结果均通过。
- 用户真实桌面肉眼检查反馈目前基本没有问题；当前没有已知验收阻断项。
- 为保留产品负责人的最终决定权，MD-005 暂保持“待验收”，等待用户明确确认后再完成归档。

### 阶段 1 正式收口与 MD-008 启动探索

- 用户认可阶段收口规划，并确认 MD-005 已跑通；MD-001、MD-002、MD-003 的原始验收项已由后续版本持续覆盖，MD-001 至 MD-007 现均完成并归档。
- 阶段 1 正式标记为“已完成”。当前稳定基线包含透明单面板、正式趴姿角色、五档尺寸、程序化呼吸、眨眼、点击弹起、动态 Hide/Show、尺寸持久化及英文/简体中文本地化。
- 阶段 2 进入探索。已建立草拟任务 `MD-008`，聚焦有限鼠标接近回应；当前只做体验、素材和事件边界确认，尚未安排开发。
- 用户提出并确认尾巴也应参与接近反馈。MD-008 已把尾巴反馈纳入核心体验，当前推荐以注意基础帧和尾巴强调帧形成一次短促回应，具体抬眼/抬头与尾巴轻抬/轻摆方向仍待用户选择。
- 用户确认 MD-008 组合方案：轻微抬头加耳朵注意、尾巴尖轻抬、短暂回应后自行恢复待机，并接受以注意基础帧和尾巴强调帧两张候选母帧开展视觉探索。
- 已生成 MD-008 第一轮两张 1254 px 视觉候选并保存到独立 workbench。当前候选为不含 Alpha 的棋盘格概念图，只用于评审头部与尾巴方向，不进入产品。
- 尾巴第二轮先产生一张整段抬起过高的幅度上限候选，随后收敛为仅外段上扬的 v3；v3 在 120 px 下仍清楚可辨，等待用户判断其弧度是否自然。
- 用户指出 v3 尾根与身体断开，项目立即撤回对 v3 的推荐并标记为无效候选。后续从连接完整的高幅度版本向下收敛，生成 v4：尾根连续、幅度降低且 120 px 下仍可辨，等待用户评审。
- 用户确认锁定 MD-008 视觉方向并进入正式透明母帧稳定制作方案阶段。已建立独立方案文档，定义两张母帧、版本化头部/尾巴/尾根蒙版、许可区外像素稳定、尾根连续性测试、五档确定性生产和停止条件；当前仍未安排产品代码开发。
- 用户正式授权进入透明母帧稳定制作。已建立 `md008-attention-masks-v1.json`，定义 `attentionHead`、`tailMotion` 与 `tailRootAnchor` 三个版本化许可区域，并生成叠加预览供正式局部来源制作前确认；尚未生成或升格透明母帧。
- 用户评审指出首版 `attentionHead` 未完整包住头部、`tailRootAnchor` 覆盖臀部过大。已按反馈扩大头部许可区的轮廓与羽化余量，并把尾根硬锚点收窄到臀部与尾巴的连接桥，等待第二版叠加预览确认。
- 用户确认第二版区域并正式锁定为 `md008-attention-masks-v1.json`。已开始按测试先行建立母帧画布、许可区外像素稳定和尾根锚点稳定约束；下一步制作两张透明局部来源并合成正式候选。
- 内置图像工具已生成 attention-base 绿键来源和两轮 tail 来源，透明去背后进入稳定合成。第一轮尾巴幅度过高被自动淘汰；第二轮虽位于许可区内，但与固定基底存在占位漂移，目视检查发现臀部空洞，因此同样淘汰并保留为失败记录。尾巴正式候选改用原 v0.4 尾巴像素做确定性外段抬升，避免身份漂移、绿边和臀部接缝。
- 确定性尾巴处理先后验证了整块旋转与连续形变：整块旋转在尾根外侧产生三角接缝；连续形变虽无断裂，却只能形成整体倾斜，不能自然重建已确认的尾尖抬起弧线。两版均未升格。默认内置绿键路径至此未达到正式毛发母帧标准；若继续，应由用户明确确认切换到需要 `OPENAI_API_KEY` 的 CLI `gpt-image-1.5` 原生透明高保真编辑路径。
- 用户提醒并重新确认既有工具决策：此前 `gpt-image-1.5` CLI 只完成命令校验，真实 API 从未调用；随后已明确放弃付费图片 API，改用 ChatGPT/Codex 内置 GPT 图像能力与本地后处理。MD-008 继续遵守该决定，不向外部 Image API 上传母版、蒙版或提示词。2026-07-20 的 CLI 尝试在上传前被安全策略拦截，没有发送项目素材。
- 继续使用内置 GPT 生成了独立“尾根短段＋尾巴外段”绿键部件，成功避免整只角色重绘。透明化与固定基底合成验证发现锁定的 v1 `tailMotion` 未完整覆盖原待机尾巴左下尾尖；由于许可区外必须逐像素不变，旧尾尖无法清除，新部件必然重影。当前合成候选淘汰。继续正式制作需要由用户确认建立 v2：仅把 `tailMotion` 向左下扩展以包含完整旧尾巴联合轮廓，`attentionHead` 与 `tailRootAnchor` 保持不变。
- 用户允许继续后已建立 `md008-attention-masks-v2.json`：仅扩展 `tailMotion`，`attentionHead` 与 `tailRootAnchor` 坐标逐点沿用 v1；v1 文件保留。第一张 U 形局部尾巴部件因折返视觉淘汰；第二张单弧线部件已完成本地透明化与稳定合成。像素诊断确认旧待机尾巴已清除、当前只有一个新 Alpha 组件，4 项母帧稳定测试通过；其较宽根部与折线观感仍待多背景人工评审，尚未升格正式母帧。
- 单弧线部件经过本地压低、向左嵌入与臀部前景遮挡调整后，尾根从红色硬锚点之后以自然厚度露出；浅色和深色棋盘评审图中只显示一条尾巴，未再出现旧尾巴重影或硬裁切。4 项稳定测试持续通过。当前为 stable-v1 待验收候选，尚未升格正式 master、生成五档素材或接入 Catalog。
- 用户多背景评审指出右上、右下臀部附近仍有旧尾巴残片，且新尾巴左下连接短段形成多余尖块并覆盖原区域。该候选退回修订：收紧臀部前景恢复区，避免恢复任何旧尾巴像素；上移局部部件，把连接短段藏在臀部与硬锚点之后。
- 用户进一步准确定位根因：原待机尾巴遮挡了部分后背和后腿，清除尾巴后暴露的是从未存在过的身体像素，不能通过继续裁尾修复。已用内置 GPT 生成无尾巴的完整后背与后腿重建来源，并建立 v3 `bodyReconstruction` 区域；红色硬锚点缩到未受遮挡的实体臀部。首轮 v3 身体恢复完整，但前景补片遮住过多尾根造成悬空，继续通过加深尾巴与身体的重叠修正连接。
- 分层平移仍无法同时满足完整身体与自然尾根后，改用内置 GPT 将无尾巴完整身体和单弧线尾巴整合为单一局部来源，并进行一次仅压低尾巴幅度的定向修订。最终 stable-v1 候选在 v3 `tailMotion + bodyReconstruction` 联合许可区内一次合成：后背毛发完整、后腿轮廓完整、尾根连续、仅一条尾巴，红色硬锚点最后恢复。浅色/深色评审图未再显示用户标记的缺口或覆盖块，4 项稳定测试通过；仍待用户人工确认，未升格正式 master。
- 用户完成最终多背景检查并确认没有问题，同意进入下一步。stable-v1 因此正式升格：生成两张 1254×1254 RGBA 母帧、两种状态各五档预览，并接入 Asset Catalog 的 `AttentionBase` 与 `AttentionTail` 分类。
- 升格后完成确定性复验：重复生成前后文件哈希一致；MD-005 与 MD-008 共 11 项素材测试通过；`git diff --check` 通过；无签名干净 Debug 构建成功。MD-008 更新为“待用户安排”，产品代码尚未启动。
- 用户于 2026-07-20 正式安排 MD-008 开发。任务状态更新为“开发中”，开发边界锁定为无需额外权限的二元接近检测、进入/离开滞回、单次 `attention-tail → attention-base → idle` 回应、离开加冷却后重触发，以及点击/拖动/Hide-Show 优先级；不重新制作素材、不引入网络、AI、第三方依赖或复杂行为系统。
- Codex 开发部门完成 MD-008 并提交 Handoff。高级项目经理独立核对任务书、Handoff、代码差异与测试覆盖，未发现阻止验收的问题；实现采用可见期 `NSEvent.mouseLocation` 定时采样，没有事件监视器或新权限，接近检测职责与视觉状态机保持分离。
- 独立新鲜复验：MD-005 与 MD-008 素材测试 11/11 通过；全部有效 `MochiDockTests` 串行运行退出码 0；无签名干净 Debug 构建退出码 0；`git diff --check` 通过。MD-008 更新为“待验收”，等待用户完成三档视觉、边缘/冷却、拖动/点击、Hide/Show 与长期低打扰人工检查。

### MD-008 开发部门交付

- Codex 已按测试先行完成有限接近检测、尺寸缩放的进入/离开滞回、4 秒冷却、单次 `attention-tail → attention-base → idle` 序列、点击抢占、拖动抑制及 Hide/Show 生命周期接线。
- 实现使用 0.15 秒的 `NSEvent.mouseLocation` 读取，不安装全局事件监听、不申请系统权限；鼠标按下/抬起只由现有面板自身事件桥接。
- MD-005 与 MD-008 共 11 项素材测试通过；全部有效 `MochiDockTests` 串行运行退出码 0；无签名干净 Debug 构建退出码 0；`git diff --check` 通过。
- 本地冒烟确认 Debug 应用可启动、120 pt 宠物面板可见且没有权限提示。120/240/320 三档动作辨识度、真实边缘频率、拖动/点击竞争及长期低打扰表现仍需人工验收。
- 已建立交付记录：`docs/handoffs/2026-07-20-MD-008-limited-pointer-proximity-response.md`。MD-008 保持 active，未由开发部门标记完成或归档；没有自动开始下一任务。

### MD-008 正式验收与归档

- 用户于 2026-07-20 明确确认 `MD-008 验收通过`，覆盖真实桌面人工体验结论。
- MD-008 状态更新为“已完成”，任务书从 `docs/tasks/active/` 移入 `docs/tasks/completed/`，项目索引同步更新。
- 当前没有开发中的编号任务，也未自动启动下一任务；后续根据真实使用体验再由用户选择阶段 2 的下一项小型改进。

### 阶段 2 后续开发计划提案

- 用户说明自己没有桌面宠物开发经验，并委托高级项目经理制定后续开发计划。
- 已核对项目记录、任务规则、当前产品代码和测试结构；MD-001 至 MD-008 全部完成，Git 工作区启动时干净，产品 Swift 文件最大 264 行，当前不需要预防性重构。
- 已建立 `docs/stage-2-development-plan.md`。推荐 MD-009 聚焦宠物位置保存和屏幕变化后的安全恢复；后续候选为低打扰控制、启动与日常可达性、阶段 2 收口试用。
- 由于窗口位置保存属于新增数据持久化，本轮将 MD-009 保持为待用户确认的推荐方向，没有创建任务书、修改产品代码或启动开发。
- 用户补充“屏幕边缘躲藏与偷看”想法：未来宠物可主动藏到屏幕外，只露出脑袋观察用户。已按用户要求加入想法池，明确暂不实施且不扩大 MD-009 范围。

### MD-009 任务建立

- 用户正式同意在本机保存宠物窗口位置，并批准将“宠物位置保存与安全恢复”定义为 MD-009。
- 已建立完整任务书 `docs/tasks/active/MD-009-pet-position-persistence-and-safe-restoration.md`，状态为“待用户安排”。
- 范围覆盖拖动结束保存、启动直接恢复、无效值回退、负坐标与多屏 `visibleFrame` 安全校正、尺寸变化、运行中屏幕配置变化和 Hide/Show 位置保持。
- 屏幕边缘躲藏与偷看继续保留在想法池；本任务的普通保存位置必须完整可见，不实现主动越界行为。
- 本轮只修改项目管理文档，没有修改产品代码、运行开发测试或自动启动 MD-009。

### MD-009 首轮交付独立复核

- 已核对任务书、工作区差异、实际产品代码、测试和实施计划；未发现新增权限、网络、依赖、数据库、素材或受保护工程配置变化。
- 新鲜独立验证：全部有效 `MochiDockTests` 串行无签名运行退出码 0；MD-005 与 MD-008 素材稳定测试 11/11 通过；全新 DerivedData 无签名 Debug clean build 退出码 0；`git diff --check` 通过。
- 独立代码审查发现一个 Important 屏幕通知顺序问题：若 AppKit 在通知前已移动窗口，当前实现不会把安全的新 origin 同步到偏好，旧屏幕坐标会残留并影响后续恢复。
- MD-009 退回“开发中”，需要增加 AppKit 预移动顺序回归测试，并在当前 origin 与已保存 origin 不一致时更新保存值；当前不进入人工验收，也不启动下一任务。

### MD-009 第二轮交付独立复核

- 开发部门已修正首轮 Important：新增测试真实模拟 AppKit 在通知前预移动 panel；实现不会重复移动安全 frame，并会把有效旧保存坐标同步为当前位置。
- 新鲜独立验证再次通过：全部有效 `MochiDockTests` 串行无签名退出码 0；素材稳定测试 11/11 通过；全新 DerivedData 无签名 Debug clean build 退出码 0；`git diff --check` 通过。
- 第二轮独立代码审查发现相邻 Important 条件：缺失或无效位置解码为 `nil` 后，未变化的屏幕通知也会因 `nil != safeFrame.origin` 写入新位置，违反无需移动不写入的任务要求。
- MD-009 继续保持“开发中”。需把旧坐标同步限定为“存在有效持久化 origin 且不一致”，并为缺失/无效值＋未变化通知增加零写入回归测试；人工验收继续暂缓。

### MD-009 第三轮交付独立复核

- 开发部门已修正第二轮相邻条件：旧坐标同步只在有效持久化 origin 与最终安全 origin 不同时触发；缺失/无效值＋未变化通知的零写入测试已加入。
- 新鲜独立验证通过：全部有效 `MochiDockTests` 串行无签名退出码 0；素材稳定测试 11/11 通过；全新 DerivedData 无签名 Debug clean build 退出码 0；`git diff --check` 通过。
- 最终独立代码审查发现两个 Important：空屏幕信息下 pending 恢复会被尺寸切换清除并被未校正居中值覆盖；新增零写入测试读取 `NSScreen.main`，不满足测试隔离要求。
- MD-009 继续保持“开发中”。需补充空屏幕→Show→尺寸切换→屏幕恢复通知的确定性测试与实现，并移除测试对真实主屏的依赖；人工验收继续暂缓。

### MD-009 第四轮交付与运行暂停诊断

- 开发部门已修正空屏幕尺寸切换丢失 pending 和测试依赖 `NSScreen.main` 两项缺口；`MochiDockTests` 中 `NSScreen` 搜索为空。
- 用户在 Xcode 运行时看到调试器停在 `swift_willThrow`。截图显示 Swift Error Breakpoint 启用；21:54 截图后没有新的 MochiDock 崩溃报告。干净 Debug 构建通过正常应用路径启动后进程持续存活且无新 `.ips`，随后只终止了本次专用实例。
- 既有 15:49 与 20:44 崩溃报告均为 AppKit 注册应用阶段 `SIGABRT`，没有进入 MochiDock 产品逻辑；本次暂停不支持“MD-009 产品崩溃”的判断。
- 新鲜独立验证：非沙箱串行全部有效 `MochiDockTests` 退出码 0；素材稳定测试 11/11 通过；无签名干净 Debug build 退出码 0；`git diff --check` 通过。首次沙箱测试宿主 `No such process` 未进入断言，不计作功能结果。
- 最终代码复审发现一个 Important 规格偏差：pending 尺寸重建保持 origin，不是任务书要求的中心点；现有离屏 clamp 测试掩盖该差异。MD-009 继续“开发中”，需增加不 clamp 的中心保持测试并修正实现。
- 用户在 Xcode 中点击继续执行后应用正常运行，实际确认 `swift_willThrow` 只是 Swift Error Breakpoint 暂停；该运行疑问关闭，不作为 MD-009 崩溃缺陷。
- 用户随后完成人工位置保存检查并确认没有问题，授权提交当前 MD-009 代码；本次提交不推送、不启动下一任务。提交前新鲜复验中，全部有效 `MochiDockTests`、MD-005/MD-008 共 11 项素材稳定测试和无签名干净 Debug 构建均通过；完整 scheme 仍仅因既有空 `MochiDockUITests` bundle 缺少可执行文件而失败。pending 恢复期间切换尺寸的中心点语义继续作为已知 Important 边界保留，不记为已修复。
- 用户进一步确认已完整检查 MD-009，暂时没有发现问题，并作出最终验收决定。MD-009 状态更新为“已完成”并归档；pending 恢复期间切换尺寸的中心点差异按用户当前体验接受为低概率已知边界，后续只有真实使用触发时再单独安排修订。当前不启动下一任务。

### MD-010 范围确认

- 用户于 2026-07-21 确认 MD-010：菜单中关闭鼠标接近回应并保存选择；让 MochiDock 随 macOS 登录自动启动。
- 已建立任务书 `docs/tasks/active/MD-010-low-interruption-and-login-start.md`，状态为“待用户安排”；本轮只确认产品范围，不启动开发。
- MD-010 验收后推荐长期使用当前版本并进行阶段 2 稳定收口；没有真实体验证据前，不自动增加新功能。

### MD-010 启动前复核与正式安排

- 已复核任务书、项目总览、阶段计划、Git 状态，以及当前菜单、偏好、接近检测、面板控制、测试结构和 deployment target；没有发现阻止开发的问题。
- 补充两项验收边界：关闭接近回应时取消已经开始的注意动作；`SMAppService` 的真实登录项启停必须使用签名应用人工验证，并显式处理“需要用户批准”等系统状态。
- 当前产品 Swift 文件最大 266 行，无需预先重构。用户已正式安排 MD-010，任务状态更新为“开发中”；下一步由 Codex 开发团队按任务书测试先行实施。

### MD-010 开发交付

- 按测试先行完成接近回应偏好、采样生命周期、活动注意动作取消、菜单连接、`SMAppService.mainApp` 登录项边界、四态同步、幂等与错误路径，以及英文/简体中文文案。两个 RED 均为预期缺失接口导致的编译失败；聚焦 GREEN 最终退出码 0、`TEST SUCCEEDED`。
- 全部有效 `MochiDockTests` 在测试拆分后的新鲜无签名运行退出码 0、`TEST SUCCEEDED`；MD-005/MD-008 素材稳定测试 11/11 通过；全新 `/tmp` DerivedData 的无签名 Debug 构建显示 `CLEAN SUCCEEDED`、`BUILD SUCCEEDED`；最终 `git diff --check` 通过。
- 首次完整回归被沙箱拒绝写入 Xcode DerivedData/日志，首次干净构建被沙箱阻断 Swift 宏插件并返回 malformed response；两次均未作为产品结果，随后在允许 Xcode 正常运行的环境使用相同命令重跑并通过。
- 无签名应用已本地实际启动，显示单一宠物且普通待机正常；界面控制工具无法访问 `MenuBarExtra` 内部开关，因此没有完成真实菜单关闭/重开接近回应的可视操作。自动测试已覆盖停止、恢复、未武装重启和活动注意动作取消，但菜单点击观感仍列入人工验收。
- 尚未执行真实登录项启用/停用、系统设置批准、注销/重新登录、单实例自动启动及自动启动后的尺寸/位置/接近偏好恢复；这些必须由签名 Xcode 运行版本完成，当前无签名测试与构建不作为该系统级证据。
- MD-010 保持“开发中”等待用户验收；未归档、未提交、未推送、未启动下一任务。

### MD-010 独立复核与设置窗口方向

- 2026-07-22 独立检查 MD-010 任务书、实际代码差异、测试和项目记录，没有发现阻止人工验收的代码问题。
- 新鲜复验：全部有效 `MochiDockTests` 共 105 项通过并显示 `TEST SUCCEEDED`；MD-005/MD-008 素材稳定测试 11/11 通过；无签名 clean build 显示 `CLEAN SUCCEEDED`、`BUILD SUCCEEDED`；`git diff --check` 在复核前通过。
- MD-010 仍缺真实菜单点击、接近回应关闭/重开视觉闭环，以及签名版本登录项启停、批准、重登和单实例恢复检查，因此状态继续为“开发中”，未验收、未归档。
- 用户提出并确认独立设置页面方向，以避免状态栏菜单持续膨胀。推荐菜单只保留隐藏/显示、设置和退出，把尺寸、接近回应和登录时启动移入单一设置窗口；该工作作为 MD-010 验收后的独立候选，不扩大当前任务。

### MD-011 双栏预览反馈与开发待办

- 用户确认采用左侧栏目、右侧内容的设置结构，并要求宠物尺寸由五段按钮改为具有五个固定圆点的吸附式细滑块。
- 已把五档映射、拖动吸附、圆点点击、键盘逐档切换、现有持久化复用和双语视觉验收写入 MD-011 任务书与实施计划。
- 产品实现继续由 Codex 开发团队负责；本次项目管理更新只修改项目文档，没有修改、撤销或提交现有产品代码。
- MD-011 保持“开发中”，开发团队还需同时修正既有设置窗口激活/前置缺口，完成测试与构建后重新交付独立复核。

### MD-011 吸附滑块与窗口前置修订启动

- 已完整复核 MD-010/MD-011 任务书、MD-011 实施计划、项目记录、当前未提交差异、双栏设置页、设置窗口控制器、尺寸枚举与相关测试。
- 当前实现保留双栏和三项设置，但尺寸仍为五段式 Picker；`openSettings()` 缺少显式应用激活。下一步先新增纯离散滑块模型与可注入激活边界的失败测试，不先修改产品实现。

### MD-011 吸附滑块与窗口前置修订交付

- 第一轮 RED：聚焦构建因缺少 `PetSizeSliderModel`、`SizeSliderMove` 及激活注入边界而失败，符合新增行为尚不存在的预期。
- 第一轮 GREEN：新增纯五档映射、最近档吸附、合法圆点选择、越界拒绝、键盘逐档移动和基于真实 Binding 的细轨道 UI；聚焦滑块与 AppDelegate 套件通过。
- 第二轮 RED：新增 AppDelegate 尺寸发布测试后，完整 AppDelegate 套件明确仅该测试失败，证明模型虽已更新但设置页观察源没有发布变化。
- 第二轮 GREEN：`selectDisplaySize(_:)` 在真实档位变化后发送一次 `objectWillChange`；聚焦 AppDelegate 与滑块套件全部通过。
- 设置打开路径新增可注入 `ApplicationActivating`，生产使用当前 SDK `NSApplication.activate()`；测试确认重复打开激活两次但继续复用同一窗口。
- 新鲜全量有效 `MochiDockTests` 退出码 0，包含 Localization、MD-010 接近回应与登录项，以及全部滑块/窗口测试。
- MD-005/MD-008 素材稳定测试运行 11 项、全部通过；无签名 Debug clean build 输出 `CLEAN SUCCEEDED` 与 `BUILD SUCCEEDED`。
- `xcresulttool` 对显式 `/tmp` 结果包汇总仍因 TestReport 内部写权限失败；随后使用完整 `xcodebuild test -quiet` 复跑并取得明确退出码 0，不依赖汇总工具作成功判断。
- MD-011 更新为“待验收”；未验收、归档、提交、推送、发布或启动后续任务。

### MD-011 第二轮人工验收退回

- 用户实测发现滑块圆点只能点击，不能从圆点开始拖动；同时滑块获得焦点时出现包围整个控件的蓝色矩形，窗口失去激活后矩形才消失。
- 独立只读复核确认：圆点按钮覆盖底层轨道手势是拖动命中失败的根因；根容器 `.focusable()` 的默认焦点效果是蓝色矩形的来源。
- 现有自动测试只证明吸附数值映射正确，没有驱动真实 SwiftUI 拖动事件，因此开发交付中的拖动完成结论证据不足。
- MD-011 从“待验收”退回“开发中”。已补充开发任务：统一轨道与圆点的拖动命中区域、保留点击和键盘操作、移除整块默认焦点环，并要求真实鼠标人工复验。
- 本轮只修改任务书、实施计划和项目记录，没有修改产品代码、测试、资源或工程配置。

### MD-011 多桌面空间行为确认

- 用户选择第一种处理方式：设置窗口重新打开时移动到当前活动的 macOS 桌面空间，不再跳回窗口上次所在桌面。
- 已把单窗口复用、设置窗口不得加入全部桌面、宠物面板跨桌面策略不变，以及“桌面 2 关闭→桌面 1 重开”的人工验收路径写入任务书和实施计划。
- MD-011 保持“开发中”；本轮只更新项目管理文档，没有修改产品代码或代替用户验收。

### MD-011 主线程隔离警告复核

- 新鲜无签名 Debug build 成功，但稳定输出 `MochiDockApp.swift:97` 警告：默认参数在非隔离上下文引用主线程隔离的 `NSApplication.shared`。
- 当前没有运行时跨线程或崩溃证据，但警告由 MD-011 新增测试注入边界引入，且可能在更严格 Swift 6 检查下升级为错误，因此列为验收前修订项。
- 已要求开发团队在明确的主线程初始化路径内组装生产激活器，保留测试注入，不得使用警告抑制或降低并发检查。
- 本轮只进行只读代码检查、新鲜构建和项目文档更新，没有修改产品实现。

### MD-011 主线程隔离修订独立复核通过

- 独立代码检查确认默认参数中的 `NSApplication.shared` 已移除，生产组装位于 `@MainActor` 初始化器内部，测试激活器仍可显式注入。
- 未发现不安全隔离、警告抑制、并发设置降级或工程配置修改。
- 新鲜 AppDelegate 聚焦测试和全部有效 `MochiDockTests` 成功；新鲜无签名 clean Debug build 成功；`git diff --check` 通过。
- 构建输出中原 `MochiDockApp.swift:97` 主线程隔离警告已消失，没有发现新的产品源码并发警告。
- MD-011 更新为“待验收”；本轮没有修改产品代码，也未代表用户验收、归档、提交或推送。

### MD-011 第二轮验收阻塞修订交付

- 测试先行新增统一指针层回归；RED 因缺少 `PetSizeSliderInteraction` 明确编译失败，GREEN 覆盖当前圆点跨档、五个圆点分别起拖及轨道空白起拖。
- 产品控件改为一个覆盖轨道与圆点的 24 pt 指针命中层；保留五档、点击、键盘、既有选择及持久化路径，移除整块默认焦点效果并只给当前圆点轻量焦点提示。
- 真实鼠标完成当前圆点 120→240、未选圆点 80→320、轨道空白→80 三条拖动；方向键 80→120；活动设置窗口截图没有整块蓝色焦点框。
- 新鲜全部有效 `MochiDockTests`、MD-005/MD-008 素材稳定测试 11/11、无签名 clean Debug build、`git diff --check` 通过。未提交、推送、验收、归档或启动后续任务。

### MD-011 当前桌面空间修订交付

- 新增设置窗口 `moveToActiveSpace`、禁止 `canJoinAllSpaces` 且保持对象复用的测试；完整 AppDelegate 套件先 RED、单行窗口策略后 GREEN。
- 人工执行桌面 2 关闭设置、切换桌面 1、同一应用实例重开设置，窗口在桌面 1 直接前置，没有切回原桌面。
- 宠物面板代码及既有 `canJoinAllSpaces` 未修改；临时人工验证入口已撤回。
- 最终相关测试、全部有效测试、无签名 clean Debug build 和 `git diff --check` 通过；未提交、推送、验收或归档。

### MD-011 主线程隔离警告修订交付

- 新鲜 RED build 捕获到 `MochiDockApp.swift:97` 的 `NSApplication.shared` 默认参数隔离警告。
- 注入初始化器改为必须显式传入激活器；生产初始化器继续在 `@MainActor` 方法体内取得原生应用，没有修改并发或工程配置。
- AppDelegate 聚焦测试、全部有效测试和新鲜无签名 clean Debug build 通过；构建输出已确认目标隔离警告消失。
- 窗口激活、单窗口复用、当前 Space 行为均保持；未提交、推送、验收或归档。

### MD-012 后续路线建立

- 用户接受高级项目经理提出的路线：先收口 MD-010/MD-011，再以固定版本完成阶段 2 稳定试用，不为了保持开发节奏而继续堆叠功能。
- 已建立 `docs/tasks/active/MD-012-stage-2-stability-trial-and-closure.md`，状态为“待用户安排”，前置条件为 MD-010 与 MD-011 完成人工验收和归档。
- MD-012 计划使用 5～7 天，只记录四类真实体验；若发现问题，先由高级项目经理复核和分流，只有用户确认后才交开发团队修复。
- 屏幕边缘躲藏与偷看保留为试用后的优先候选，但未创建后续编号、未安排素材或代码开发。
- 本轮只更新项目管理文档，没有修改产品代码、测试、资源或工程配置，也没有代表用户验收 MD-010/MD-011 或启动 MD-012。

### MD-010/011 验收与 MD-012 正式启动

- 用户于 2026-07-22 明确确认 MD-010 与 MD-011 验收结束，授权归档两项任务并开始 MD-012。
- MD-010 与 MD-011 状态更新为“已完成”，任务书移入 `docs/tasks/completed/`。
- MD-012 状态更新为“开发中”；下一步由开发团队固定 `f85ef0c` 产品基线并执行新鲜测试、素材验证、无签名 clean Debug build 和差异检查。
- 本次授权不包含新增产品功能；5～7 天真实使用仍由用户完成，高级项目经理负责问题分流、进度统筹和最终收口复核。

### MD-012 固定基线验证完成

- 开发团队固定 `main` 分支产品基线 `f85ef0ca578e54c5169ed3eb250a5ea3a3b34b9c`（`feat: add low-interruption settings window`），与 `origin/main` 一致；产品目录没有未提交差异。
- 新鲜运行全部有效 `MochiDockTests` 退出码 0；MD-005/MD-008 素材稳定测试 11/11 通过；无签名 clean Debug build 退出码 0；`git diff --check` 通过。
- 验证过程没有修改产品代码或产生仓库文件变化；当前未提交内容仍仅为高级项目经理的任务状态、归档和路线文档。
- MD-012 已具备开始 5～7 天固定版本试用的条件。建议观察窗口为 2026-07-22 至 2026-07-27～29；期间只记录打扰、恢复/启动异常、最喜欢的互动和最希望改善的一件事。
- 开发团队的交付信号只代表“启动验证完成”，不代表 MD-012 整体已完成；任务继续保持“开发中”，等待真实使用和高级项目经理收口复核。

### MD-012 提前收口与阶段 2 完成

- 用户反馈当前没有太多建议，未报告阻塞性打扰、消失/恢复/启动异常或必须立即改善的问题，并决定提前进行收口评估。
- 高级项目经理结合用户体验结论、固定基线和新鲜自动验证，建议接受阶段性收口；明确保留“实际观察期短于计划 5～7 天”的证据限制。
- 用户的收口决定使 MD-012 更新为“已完成”并归档，阶段 2 同步标记为“已完成”。
- 当前没有已安排的下一项开发任务。屏幕边缘躲藏与偷看、小型工具和 AI 仍为未承诺方向，不自动建立 MD-013。

### MD-013 方向确认与待安排

- 用户于 2026-07-22 明确选择“屏幕边缘躲藏与偷看”；该方向获得产品确认，正式建立 MD-013。
- 完成只读工程核对：现有单一面板、完整可见位置持久化、屏幕安全校正、指针接近采样和可取消动画调度可承接最小实验，不需要新增权限、网络、数据库、依赖或素材。
- MD-013 首轮限定为用户拖到左右外边缘后才自动缩进；中央自主移动、上下边缘、相邻显示器接缝、多屏穿越、设置和随机行为明确排除。
- 已建立任务书 `docs/tasks/active/MD-013-edge-hide-and-peek.md` 和测试先行实施计划 `docs/superpowers/plans/2026-07-22-md-013-edge-hide-and-peek.md`，任务状态为“待用户安排”。
- 用户纠正协作方式：高级项目经理只提供开发 Prompt，不替用户启动 Codex 开发任务；用户将亲自发送 Prompt，以便查看并控制每一步。
- 本轮只修改项目管理和实施计划文档，没有修改产品 Swift、测试、素材、工程配置或现有持久化数据；下一步等待用户把开发 Prompt 交给 Codex 开发团队。

### MD-013 首轮交付独立复核

- 开发团队完成代码与自动验证交付；任务保持“开发中”，未提交、推送、归档或标记完成。
- 高级项目经理新鲜复跑全部有效 `MochiDockTests`，145/145 通过；MD-005/MD-008 素材稳定测试 11/11 通过；全新 `/private/tmp/md013-review-build-019f897e` 无签名 clean Debug build 成功；`git diff --check` 通过。
- 独立代码复核发现三个 Important：真实 `mouseDown → tap` 顺序会使露头点击只回位而丢失开心回应；`.returning` 无法被 Hide、尺寸/屏幕变化或再次拖动安全取消；等待/偷看时关闭装饰性接近回应会错误关闭安全靠近唤回。
- 审查提出的“拖动后指针原地停留不会取消等待”不构成当前任务书偏差：没有新的有效靠近转换，且任务目标本身要求拖到边缘后等待缩进。该体感留到真实体验判断。
- MD-013 暂不进入人工验收，继续保持“开发中”。下一步由用户把修订 Prompt 交给 Codex 开发团队；修正后需补确定性回归并重新完整交付。

### MD-013 第二轮交付独立复核

- 开发团队修订首轮三个 Important 后再次交付；高级项目经理新鲜复跑 155/155 全部有效测试、11/11 素材稳定测试、全新 `/private/tmp/md013-rereview-build-019f897e` 无签名 clean Debug build 和 `git diff --check`，结果均通过。
- 独立复核确认真实点击的单次开心回应与关闭装饰性接近后的安全唤回已经关闭；Hide、尺寸和屏幕变化中断返回也已由世代失效和 animator 取消保护。
- 仍有一个 Important：从 `.peeking` 直接按住拖动时会先启动异步回位动画，可能与用户拖动竞争。现有再次拖动测试从 `.returning` 开始，未覆盖直接露头拖动。
- MD-013 继续保持“开发中”，暂不进入人工验收；下一步由用户发送聚焦修订 Prompt，补直接 `.peeking → mouseDown → drag → mouseUp` 的确定性 RED/GREEN 后重新交付。

### MD-013 开发启动

- 用户于 2026-07-22 正式安排 MD-013；任务状态由“待用户安排”更新为“开发中”，不启动后续任务。
- 开发前只读核对确认：`main` 比 `origin/main` 超前 1 个已有文档提交；三份根目录项目记录已有未提交变更，MD-013 任务书与实施计划为未跟踪文件；本任务保留并只做必要增量更新。
- 现有产品没有未提交差异；单一 `NSPanel`、单一 `PetInteractionModel`、可取消调度、完整可见位置持久化与指针采样边界均与任务计划一致；无需新权限、依赖、素材或持久化键。
- 启动基线验证：无签名、串行的全部有效 `MochiDockTests` 共 122 项通过，输出 `TEST SUCCEEDED`。
- 已确认两个集成风险：偷看期间的安全靠近唤回必须不受装饰性接近开关影响，但完整可见时仍尊重 MD-010 的停止采样语义；尺寸或屏幕变化必须先退出临时越界，再进入既有完整可见安全校正。两项均可在现有范围内解决，不构成阻塞。

### MD-013 阶段 1：纯边缘几何

- RED：新增独立 `PetEdgePlacementTests` 后，聚焦构建因缺少 `PetEdgePlacement` 和 `PetScreenEdge` 失败，退出原因与预期缺失能力一致。
- 最小实现：新增纯计算策略，集中定义 24 pt 触发带、`max(显示尺寸 × 0.35, 32 pt)` 露出宽度，只接受完整包含的 frame，并按面板垂直跨度排除相邻显示器内部接缝。
- GREEN：聚焦几何套件通过；左右外边缘、中央/上下拒绝、内部接缝、负坐标显示器、五档露出宽度和精确恢复原完整 frame 均通过；复跑无新增产品源码警告。
- 本阶段未读取 `NSScreen`、未移动面板、未写入偏好，与任务书无偏差。

### MD-013 阶段 2：可取消边缘状态协调

- RED：独立协调器测试首次还暴露测试文件缺少 `Foundation` 导入；先修正该装配问题后复跑，失败只剩 `PetEdgeBehaviorCoordinator` 及阶段类型缺失，符合预期。
- 最小实现：新增聚焦状态协调器，持有一个 6 秒等待任务；以任务取消加世代令牌阻止过期回调，并以 `returning` 状态确保点击回应只在完整 frame 恢复完成后请求一次。
- GREEN：拖动结束武装、等待到期、指针/点击/新拖动取消、过期回调、靠近恢复、点击恢复后单次回应、重复输入幂等以及 Hide/几何变化恢复套件全部通过。
- 协调器不引用 AppKit、偏好或真实屏幕，不提供持久化回调，与任务书无偏差。

### MD-013 阶段 3：偷看注意视觉生命周期

- RED：聚焦模型测试因 `enterEdgePeekVisual()` 与 `leaveEdgePeekVisual()` 缺失而构建失败，无其他装配或环境错误。
- 最小实现：`PetInteractionModel` 仅新增私有偷看生命周期标记和两个显式方法；进入时取消现有 `scheduledTask` 并固定复用 `attentionBase`，离开时幂等恢复普通 idle 调度，`stopPlayback()` 同步清除。
- GREEN：新增模型套件与既有 `PetAnimationTests` 一并通过；进入取消自动动作、无新循环持有注意基础帧、单次恢复、离开后原点击回应及停止播放清理均通过。
- 未新增视觉状态、素材、模型或调度器，与任务书无偏差。

### MD-013 阶段 4：单一面板接入与验证

- RED：独立 `PetPanelEdgeBehaviorTests` 在修正测试中 `NSPanel`/`PetPanel` 静态类型后，因控制器尚缺边缘调度与 frame 动画注入、安全靠近入口及点击入口而失败，符合预期。
- 最小实现：`PetPanelController` 组装纯几何与协调器，仍是唯一 panel frame 和 `pet.windowPosition` 写入者；普通缩进/唤回使用 0.22 秒 `easeInEaseOut`，Hide、尺寸和屏幕异常路径先立即恢复完整 frame 再走既有安全规则。
- 关闭装饰性接近回应时，只在边缘等待/偷看期间临时启动现有指针采样，恢复完整可见后停止；未建立第二指针循环。
- GREEN：新面板套件及既有面板、模型、动画、接近、位置和偏好套件通过；左右缩进、中央/内部接缝拒绝、零偷看位置写入、关闭装饰性接近时唤回、点击返回后单次开心回应、Hide/Show、尺寸/屏幕恢复和面板身份均通过。
- 完整回归首轮在 145 项中暴露 1 项重复生命周期信号：关闭装饰性接近时，靠近唤回同时由动画完成和入口重复调用幂等 `stop()`。根因追踪后新增同类点击 RED，最小修正改为按事件前阶段分配唯一停止责任；靠近与点击聚焦套件随后 GREEN。
- 最终自动证据：无签名串行全部有效 `MochiDockTests` 145/145 通过；MD-005/MD-008 素材稳定测试 11/11 通过；全新 `/tmp/mochidock-md013-build.UzsRtm` 派生目录 clean Debug build 输出 `CLEAN SUCCEEDED` 与 `BUILD SUCCEEDED`；`git diff --check` 通过。
- 职责复核后将既有 `PetPanel` 输入桥接拆到独立文件；`PetPanelController.swift` 最终 394 行，`PetInteractionModel.swift` 318 行，其他新产品文件均不超过 149 行。
- 真实桌面有效证据限于：干净 Debug 应用实际启动，只显示一个完整可见的 120 pt 宠物，点击后进入开心并恢复休息。UI 工具的拖动被识别为点击，且无法访问状态栏设置，因此左右缩进、120/240/320 露头视觉、靠近/点击唤回、Hide/Show 与重启的真实桌面闭环未被工具可靠验证，保留给用户人工验收。
- 未修改工程配置、Asset Catalog、String Catalog、偏好键、权限、网络、依赖、签名或 deployment target；MD-013 保持“开发中”，未代表用户验收、归档或标记完成。

### MD-013 首轮独立复核修订

- 修订 1 RED：新增真实 `PetPanel.sendEvent(leftMouseDown) → handlePetClick() → leftMouseUp` 与可控未完成回位动画后，普通拖动测试通过，但露头点击测试失败，证明直接调用点击的旧测试绕过了真实事件顺序。
- 修订 1 GREEN：`.returning` 中的点击只把当前回位升级为一个布尔点击请求；重复点击折叠，完整 frame 动画完成后恰好触发一次既有开心回应。
- 修订 2 RED：新增返回动画未完成时 Hide、尺寸切换、屏幕参数变化、再次拖动及旧完成强制到达测试；协调器与四条控制器路径均失败，复现恢复 frame 缺失、取消无效和旧回调竞争。
- 修订 2 GREEN：返回阶段暴露完整 frame，取消递增协调器世代并清除点击；frame 动画器新增取消和独立世代，控制器在高优先级事件前立即恢复完整 frame。聚焦协调器与面板套件全部通过，旧完成不能补发点击或覆盖新 frame。
- 修订 3 RED：等待和偷看中关闭装饰性接近回应的三条测试失败，确认检测器被提前停止且偷看 `attentionBase` 被设置切换误清除。
- 修订 3 GREEN：现有检测器以控制器侧幂等标记管理，边缘状态活跃时继续承担安全唤回，回到 inactive 后只停止一次；点击和靠近唤回均通过，未增加采样循环。
- 最终有效 `MochiDockTests` 串行复跑退出码 0；首轮 145 项加本轮 10 项确定性回归共 155 项。首次误带 UI target 的命令因 UI runner 启动失败而作废，随后按任务书限定只运行 `MochiDockTests` 成功。
- MD-005/MD-008 素材稳定测试 11/11 通过；全新 `/private/tmp/md013-revision-clean-build-escalated-20260722-1420` 无签名 clean Debug build 退出码 0；`git diff --check` 通过。沙箱内首次 clean build 因 Swift 宏插件子进程权限失败而作废，获准用同一命令在沙箱外复跑成功。
- 最终行数：`PetPanelController.swift` 412 行（已按计划把 frame 动画和输入桥接分别放入独立类型，未达到 500–600 行警戒范围）、`PetInteractionModel.swift` 318 行、`PetEdgeBehaviorCoordinator.swift` 155 行、`PetPanelFrameAnimator.swift` 33 行。
- 本轮没有取得新的真实桌面拖动闭环证据；首轮实际启动仅验证单面板、120 pt 完整可见和普通点击。左右缩进、120/240/320 露头视觉及真实 Hide/尺寸/屏幕中断仍留给人工验收。任务继续为“开发中”，未提交、推送、归档或标记完成。

### MD-013 直接从偷看状态拖动修订

- RED：受控 animator 保留尚未完成的缩进回调，从真实 `.peeking` 直接发送 mouse-down。拖到另一外边缘与拖到中央两条测试均失败：取消次数为 0、动画队列从 1 增至 2、panel 仍在偷看 frame、视觉仍为 `attentionBase`，旧回调随后可覆盖用户最终 frame。
- 最小实现：`handlePointerContactChanged(true)` 统一先调用同步完整恢复边界，再记录 `dragStartFrame`；移除 pointer-down 对 `edgeCoordinator.beginDrag()` 的动画式回位调用。没有修改协调器的点击、Hide、尺寸、屏幕或接近处理。
- 既有真实点击测试按新的同步所有权边界更新：mouse-down 后 panel 已完整可见且没有异步回位，SwiftUI tap 随后仍只触发一次既有开心回应；产品验收语义不变。
- GREEN：面板边缘套件 17 项通过。外边缘目的地仅在 mouse-up 保存一次最终完整 frame 并重新武装一个六秒等待；中央目的地仅保存最终 frame 且不重新武装。偷看与中间 frame 零写入，强制到达的旧完成不能覆盖最终 frame、补发点击或恢复偷看视觉。
- 最终验证：全部有效 `MochiDockTests` 串行退出码 0，共 157 项；MD-005/MD-008 素材稳定测试 11/11；全新 `/private/tmp/md013-direct-drag-clean-build-20260722-1512` 无签名 clean Debug build 退出码 0；`git diff --check` 通过。
- 最终行数：`PetPanelController.swift` 409 行、`PetInteractionModel.swift` 318 行、`PetEdgeBehaviorCoordinator.swift` 155 行、`PetPanelFrameAnimator.swift` 33 行；仍为单一 panel、单一 interaction model 和既有调度边界。
- 本轮为确定性 AppKit 事件测试，没有新增真实桌面人工闭环证据。任务继续保持“开发中”，未提交、推送、归档或标记完成。

### MD-013 首轮人工验收修订

- 问题 1 RED：新增靠近回位后再次缩进、Hide/Show/尺寸/屏幕单一等待、重建控制器恢复边缘位置及中央对照测试；边缘生命周期均因缺少统一重评而失败。
- 问题 1 GREEN：新增当前完整 frame 的统一重新武装入口，由回位完成、Show、尺寸和屏幕恢复调用；重复事件只留下一个有效六秒任务，中央与失去资格的 frame 保持 inactive。
- 问题 2 RED：测试注入独立 `proximityIntentScheduler` 时因接口不存在编译失败，证明当前没有可控延迟边界。
- 问题 2 GREEN：新增 0.25 秒可取消靠近意图门控；覆盖延迟到期回位、重复 entry 单任务、真实 mouse-down→tap 一次开心、拖到中央/另一边缘、Hide/尺寸/屏幕取消及强制旧回调失效。
- 问题 3 RED：实际 `PetPanel` 配置测试确认未声明 `.fullScreenDisallowsTiling`。最小实现只把该公开 AppKit 标志加入现有 `.canJoinAllSpaces`，聚焦配置测试 GREEN。
- 真实 Debug 应用成功启动，确认仍是单一 120 pt borderless floating panel且普通视觉正常。桌面控制首次读取超时；第二次能读取窗口，但拖动坐标映射两次失败，未能观察拖动中的瞬时平铺预览。因此不得宣称问题 3 已获真实桌面修复证据。
- 最终自动证据：全部有效 `MochiDockTests` 串行退出码 0，共 165 项；MD-005/MD-008 素材稳定测试 11/11；全新 `/private/tmp/md013-desktop-review-clean-build-20260722-1608` 无签名 clean Debug build 退出码 0；`git diff --check` 通过。
- 按清晰职责提取 `PetProximityIntentGate.swift` 31 行；`PetPanelController.swift` 465 行，未进入 500–600 行警戒范围。未新增设置、权限、依赖、素材或持久化键。
- MD-013 继续保持“开发中”；未提交、推送、归档、标记完成或启动 MD-014。

### MD-013 最终独立复核

- 高级项目经理逐项复核直接露头拖动修订及全部 MD-013 工作区差异，未发现 Critical、Important 或 Minor 问题；先前三个 Important 和第二轮直接拖动竞争均已关闭。
- 新鲜独立验证：全部有效 `MochiDockTests` 157/157 通过；MD-005/MD-008 素材稳定测试 11/11 通过；全新 `/private/tmp/md013-finalreview-build-019f897e` 无签名 clean Debug build 输出 `CLEAN SUCCEEDED` 与 `BUILD SUCCEEDED`；`git diff --check` 通过。
- MD-013 状态由“开发中”更新为“待验收”。下一步由用户完成左右边缘、三档视觉、靠近/点击、直接露头拖动、中断恢复、重启和低打扰感的真实桌面检查。
- 本轮只更新项目管理记录，没有修改产品代码；未代表用户验收，未提交、推送、归档或标记完成。

### MD-013 首轮人工验收未通过

- 用户确认 120/240/320 三档基本边缘缩进、露头和靠近回位通过。
- 用户发现靠近回位后不再自动缩进；Hide/Show、尺寸切换及退出重启恢复到外边缘时也不再自动缩进。只读代码核对确认当前重新武装入口仅在用户拖动 mouse-up，属于同一生命周期缺口。
- 用户无法真实点击或直接拖动露头宠物，因为指针进入接近范围已抢先回位；既有自动测试绕过了真实桌面上“先穿越接近区”的输入前置条件。需要以短暂可取消门控保留靠近、点击和拖动三种交互。
- 拖到屏幕边缘会触发 macOS 半屏平铺预览大框；需由开发团队先复现并核对 panel 窗口角色和公开 AppKit 配置，再作最小修正。
- MD-013 从“待验收”退回“开发中”；下一步由用户亲自把聚焦修订 Prompt 交给 Codex 开发团队。本轮未修改产品代码，未提交、推送、归档或启动 MD-014。

### MD-013 平铺预览修订复验失败

- 开发团队完成生命周期重新武装和 0.25 秒靠近门控，自动验证报告 165 项测试与 11 项素材测试通过；平铺部分只增加 `fullScreenDisallowsTiling`，且明确没有取得真实拖动证据。
- 用户随后真实复验确认半屏平铺预览仍存在，因此平铺修正不成立，MD-013 继续保持“开发中”。
- 官方 AppKit 文档核对确认该 flag 仅排除 full-screen tile。下一轮聚焦把系统托管背景拖动替换为 `PetPanel` 内应用控制的 mouse-down/dragged/up frame 移动，并以用户真实拖动作为唯一完成证据。
- 已通过的生命周期和靠近门控不重新开发；未提交、推送、归档、标记完成或启动 MD-014。

### MD-013 应用控制拖动独立复核

- 开发团队把 panel 改为不可由系统移动，并在 `PetPanel` 内用屏幕坐标 delta 自控 frame；报告的真实左右慢拖、快速甩动和跨侧露头拖动均未出现平铺预览。
- 高级项目经理新鲜复跑全部有效测试，167 个逻辑测试、16 个套件通过；开发报告的 251 是参数化测试展开后的执行次数，不是 251 个独立测试。
- 独立复核发现一个 Important：Hide、尺寸、屏幕变化只取消 panel 内部拖动坐标，没有统一清除控制器拖动起点；尺寸/屏幕路径也没有恢复检测器 dragging 状态。现有测试未覆盖真实 drag 被这些生命周期事件打断后再收到旧 mouse-up。
- MD-013 继续保持“开发中”。下一步只修订统一拖动取消边界并补三类中断回归；不重做自控拖动、平铺方案、边缘生命周期或靠近门控，不启动 MD-014。

### MD-013 拖动取消最终独立复核

- 统一取消边界已关闭上轮 Important：panel/controller 拖动起点、检测器 dragging 与靠近意图一次清除；Hide、有效尺寸和屏幕变化后的旧 dragged/up 均为空操作，新拖动不继承旧状态。
- 独立代码审查未发现 Critical、Important 或 Minor；32 项面板边缘聚焦测试通过。
- 新鲜独立验证：全部 171 个逻辑 `MochiDockTests`（参数化展开 255 次）通过；素材稳定测试 11/11；全新 `/private/tmp/md013-final-cancel-review` 无签名 clean Debug build 成功；`git diff --check` 通过。
- MD-013 从“开发中”更新为“待验收”。下一步由用户最终复测真实左右边缘平铺、自控拖动、三种唤回/拖动入口与恢复后再次缩进；未提交、推送、归档、标记完成或启动 MD-014。

### MD-013 最终人工验收与收口

- 用户完成真实桌面复测并确认目前未发现问题，MD-013 人工验收通过。
- 左右边缘不再出现 macOS 半屏平铺预览；应用控制拖动、六秒缩进、靠近/点击/直接拖动、Hide/Show、尺寸和重启后的重新等待均完成最终体验收口。
- MD-013 状态由“待验收”更新为“已完成”，任务书归档至 `docs/tasks/completed/MD-013-edge-hide-and-peek.md`。
- 本次收口只提交 MD-013 产品代码、测试、实施计划与项目记录；不推送、不发布、不启动 MD-014。

### MD-013 应用控制拖动修订

- RED：新增 panel 禁止系统移动、屏幕坐标 delta、固定起始 frame、无位移不持久化及真实事件拖动回归；当前系统托管配置和缺失 dragged 处理使聚焦测试退出码 65。全量首跑另发现一项旧测试仍要求背景可拖动，已按本轮明确边界修正。
- GREEN：`PetPanel` 集中处理真实 left mouse-down/dragged/up；mouse-down 在同步露头恢复前保存屏幕坐标，恢复后记录完整 frame；dragged 始终从该 frame 加屏幕 delta 更新同一 panel，mouse-up 沿用控制器最终安全校正、单次持久化和边缘重新武装。未调用 `performDrag(with:)`，未增加 panel、权限、依赖、设置或持久化键。
- 聚焦 `PetPanelEdgeBehaviorTests` 28 项全部通过；全部有效 `MochiDockTests` 串行退出码 0，结果包记录 251 次测试执行；MD-005/MD-008 素材稳定测试 11/11；全新 `/private/tmp/md013-custom-drag-clean` 无签名 clean Debug build 退出码 0；`git diff --check` 通过。
- 真实桌面：终止两个遗留 Debug 实例后只启动本轮最新应用。公开 CoreGraphics 实际鼠标输入完成左、右慢速边缘拖动并停留，全桌面截图未出现半屏平铺预览；快速左甩后也无残留预览。拖动连续且露头直接跨侧没有首次跳动；右边缘松手、移开指针并等待后进入 42 pt 露头位置。
- `PetPanel.swift` 49 行，`PetPanelController.swift` 469 行；控制器仍低于 500–600 行警戒范围。MD-013 保持“开发中”，未提交、推送、归档、标记完成或启动 MD-014。

### MD-013 生命周期中断拖动修订

- RED：新增真实 `sendEvent` 序列覆盖 `mouseDown → mouseDragged → Hide/尺寸/屏幕 → stale dragged/up` 以及中断后新拖动。Hide、尺寸和屏幕三条按预期失败：旧 mouse-up 额外写入位置，屏幕安全 frame 被旧拖动覆盖，尺寸/屏幕检测器未统一退出 dragging；聚焦套件退出码 65。
- GREEN：新增控制器级 `cancelActiveDrag()`，统一清除 panel 与 controller 两侧起点、检测器 dragging 和靠近意图；`PetPanel` 在会话已取消时直接忽略旧 dragged/up。Hide、有效尺寸切换和屏幕通知接入同一入口，相同尺寸提前返回语义保持不变。
- 本轮新增 4 项，既有 28 项加新增后共 32 项 `PetPanelEdgeBehaviorTests` 通过；全部有效 `MochiDockTests` 为 171 个逻辑测试、参数化展开后 255 次执行、0 失败；MD-005/MD-008 素材稳定测试 11/11；全新 `/private/tmp/md013-lifecycle-drag-clean` 无签名 clean Debug build 退出码 0；`git diff --check` 通过。
- 最终行数：`PetPanel.swift` 49 行，`PetPanelController.swift` 473 行，`PetPanelEdgeBehaviorTests.swift` 743 行。产品控制器仍低于 500–600 行警戒范围；测试文件增长来自同一 AppKit 生命周期 harness 与真实事件矩阵，本轮未为行数机械拆分。
- 本轮没有修改自控拖动算法、平铺修正、边缘重新等待或 0.25 秒门控，也未重复宣称桌面平铺验证。MD-013 保持“开发中”，未提交、推送、归档、标记完成或启动 MD-014。
