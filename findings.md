# MochiDock 已确认结论

## 产品定位

- 首个产品形态是 macOS 桌面宠物。
- 视觉和交互灵感包括早期杀毒软件桌面助手与 QQ 宠物。
- 后续希望逐步加入工具能力。
- 对 AI 有兴趣，但当前尚未确定适合的 AI 功能。
- 产品方向允许在开发和实际使用过程中逐步形成。

## 第一阶段验证重点

- 桌面显示是否稳定。
- 拖动、点击和鼠标交互是否自然。
- 动画和状态切换是否舒适。
- 是否干扰正常工作。
- 菜单、设置和退出是否清晰。
- 用户是否愿意长期保留桌面宠物。

## 项目管理原则

- 用户保留项目经理、产品负责人和最终决策者角色。
- 项目采用轻量范围管理，不引入不必要的企业流程。
- 使用“当前正在做 / 下一步可能做 / 想法池”管理优先级。
- 当前高级项目经理 Agent 每个阶段结束时汇总：已完成、当前进行、风险、阻塞、下一步和待确认事项。
- 当前高级项目经理 Agent 不直接编写产品代码；Codex 开发部门负责开发实现和技术自测。
- 需要开发时，由高级项目经理 Agent 说明应安排的开发任务并提供交付说明，用户负责决定和安排 Codex 开发任务。
- 高级项目经理 Agent 可通过只读检查进行验收，不替代开发部门实施。
- 关键产品或高风险决策仍由用户确认。

## 代码质量原则

- 500–600 行是文件规模警戒范围，而不是机械限制。
- 低耦合、单一职责和可在有限上下文中完整理解比单纯行数更重要。
- 入口与实现分离是合理方向。
- 不应为追求小文件而机械拆分，也不应建立万能工具类。

## AI 协作原则

- 不把聊天上下文当作长期项目记忆。
- 不按固定 token 百分比机械换 Agent，而是观察上下文质量是否退化。
- Handoff 记录某次任务的现场；Skill 记录同类任务以后应如何执行。
- 交接文件必须区分已确认事实、未验证推测、建议和待用户决定事项。
- 新 Agent 接管后先只读核对，不直接继续修改。
- Skill 应少而清晰、按需加载，并像代码一样接受检查和维护。

## MochiDock PM Skill 边界

- 它是 Codex 的项目管理辅助工作流，不替代用户的 PM 身份。
- 第一版应聚焦需求整理、小阶段规划、范围控制、进度与风险汇总、Handoff 和接管验证。
- 暂不建立大量角色型 Skill。
- 新增或重大修改 Skill 前，应由用户确认其是否真正通用。

## 输出体验要求

- 完整 Markdown、Codex 提示词、配置文件或较长代码应保持为单一连续内容。
- 对话界面本身的复制问题无法由 Skill 修复，但可以通过输出结构降低内容断开的概率。
- 长内容优先落盘并提供文件链接。

## 待确认事项

- 小熊猫第一轮视觉草图完成后，从候选轮廓与面部花纹中选择最终方案。
- 阶段 1 的具体交互原型和验收标准。
- 第一个值得尝试的工具能力与 AI 场景。

## 阶段 1B 已确认方向

- 用户确认阶段 1B 继续聚焦最基础的桌面宠物体验，不实现想法池中的自动走动、飞行、工具、AI、换装或成长系统。
- 用户确认将待机姿势从当前站立改为趴着；站立形象仍可作为已有角色识别基线，但不作为阶段 1B 的常驻待机状态。
- 阶段 1B 继续使用 120 px 作为默认显示尺寸，同时保留 80/120/160 三档切换供体验验证。
- 首轮待机生命感为趴姿下的轻微呼吸或身体起伏、偶尔眨眼，动作之间需要有停顿，不持续晃动。
- 首轮点击回应采用“开心弹起后自动恢复趴姿待机”的单一可验证动作。
- 鼠标响应仅作为阶段 1B 之后的可能下一步，不进入当前承诺范围。
- 用户已确认趴姿静态候选 v0.4 的姿势、与 v0.3 的角色一致性、尾巴位置和颜色；未发现需要调整的造型差异。
- 用户已确认纯本地色键与局部去绿方案 v5 的透明边缘结果；该结果已原样升格为 v0.4 趴姿正式母版，并从其确定性生成 80/120/160 px 三档透明素材。
- v0.4 素材处理期间不使用付费图片 API；角色内部像素不重新绘制，正式素材只从用户确认的本地处理结果晋升。
- 生成式编辑不适合当前的轻微呼吸帧：首个候选改变了绿幕颜色，却只产生约 1 像素的主体边界变化，120 px 下动作不可见。当前推荐验证复用同一透明图的确定性纵向缩放，以避免纹理漂移和背景闪烁；是否正式改用该实现仍待用户确认。
- 用户已确认可看到确定性透明预览的起伏，同意先试用该版本。阶段 1B 呼吸正式改为复用同一透明趴姿素材、底部锚定的程序化纵向缩放；不再制作或接入呼吸图片帧。

## 小熊猫角色母版 v0.2

- 用户已确认首版角色为圆润幼崽体型的小熊猫，性格活泼、好奇、略冒失。
- 用户已确认尾巴承担飞行功能，飞行风格为柔软、略笨拙的喜剧飞行。
- 用户已确认主毛色为稍明亮的自然红棕，眼睛为中等大小圆眼。
- 成长型外观、服装、配件和其他性格分支属于未承诺创意，不进入首版范围。
- 完整比例、固定识别点、飞行语言和禁止项见 [角色母版 v0.2](docs/design/character/character_master_v0.2.md)。

## 阶段 1A 工程核对

- 当前 Xcode 为 26.4.1，项目的 macOS 部署目标为 26.4，和本机工具链一致。
- 当前应用仍是 SwiftData 模板：`MochiDockApp.swift` 创建 `ModelContainer`，`ContentView.swift` 展示项目列表，`Item.swift` 是时间戳模型；这些都不属于桌面宠物职责。
- Xcode 工程使用文件系统同步分组，新建 Swift 文件会自动纳入对应 target，不需要手工编辑 `project.pbxproj`。
- 阶段 1A 采用 AppKit `NSPanel` 管理透明浮动窗口，SwiftUI 只负责绘制和交互状态，避免让入口文件同时承担窗口与视图职责。
- 阶段 1A 不引入持久化；是否保存宠物位置和设置留给后续阶段，由用户确认后再决定。
- `xcodebuild -list` 能识别 `MochiDock` scheme；受当前沙箱限制，命令输出包含 CoreSimulator 与日志权限警告，但 macOS 项目和 scheme 仍被成功读取。正式构建验证将在实施阶段执行。

## 阶段 1A 开发初审发现

- 产品实现采用长期持有的单一 `NSPanel`、独立互动状态模型和 SwiftUI 系统绘图，占位形象与窗口生命周期职责分离，符合任务书边界。
- 用户已确认实际启动、单一透明无边框显示，以及第一次点击后变为开心；第二次点击恢复、实际拖动、菜单恢复与退出、低打扰表现和 30 分钟稳定性仍需人工验收。
- 自动测试验证了状态往返、窗口透明/无边框/可拖动配置、重复显示复用和关闭后恢复；测试属性检查不能替代真实拖动与点击手势组合的人工验证。
- 面板测试会展示真实 `NSPanel` 但未主动清理，且缺少 `hidePet()` 后恢复的专门回归测试；两项均为非阻塞质量改进，不影响进入人工验收。

## MD-002 工程核对

- 正式角色三档资源使用独立 image set，未错误映射成同一资源的 1x/2x/3x；当前 scale 1 方案只服务体验评估，不代表最终 Retina 素材体系。
- 默认尺寸为 120；尺寸状态、资源名和 point 边长集中在 `PetDisplaySize`，面板控制器切换同一 `NSPanel` 的 frame 并保持中心。
- 高级项目经理独立复跑 22 个测试和干净 Debug 构建均成功，资源像素、alpha、哈希和构建产物 rendition 均与 Handoff 一致。
- 未发现 Critical 或 Important 代码问题。测试未主动清理真实面板是非阻塞质量项；全视图点击手势与 `isMovableByWindowBackground` 的组合必须通过真实拖动验收。
- 人工验收确认尺寸切换和 30 分钟稳定性通过。
- `PetPanelController.hidePet()` 已存在，但没有从 `MochiDockAppDelegate` 和菜单暴露；无边框面板又没有关闭按钮，导致用户无法进入隐藏状态。当前 `Show Pet` 在宠物已显示时无视觉变化属于幂等行为，真正缺口是缺少可达的 `Hide Pet` 操作。
- MD-003 首轮实现同时显示 `Show Pet` 与 `Hide Pet`。用户要求合并为单一动态菜单项：面板可见时提供 `Hide Pet`，面板隐藏时提供 `Show Pet`；不使用固定的 `Toggle Pet` 文案。
- MD-003 第二次开发报告复用了旧的 `hidePet` RED 和 27 项测试，但没有修改两个独立菜单按钮，也没有新增动态标题、单一切换入口或 Handoff 修订记录；旧测试通过不能证明新交互需求已实现。
- MD-003 最终修订使用单一动态按钮；标题直接读取 `PetPanelController.isPetVisible`（实际 `NSPanel.isVisible`），AppDelegate 的 `ObservableObject` 通知仅触发 SwiftUI 重新求值，没有保存第二份可见状态。独立复核确认固定双按钮已移除，30 个测试和干净构建通过。
- 用户一度建议固定 `Show/Hide Pet`，进一步排查后最终保留更明确的动态标题：可见时 `Hide Pet`，隐藏时 `Show Pet`。
- 启动后宠物看似未显示的根因是面板可能停留在其他 Space；菜单一直显示 `Show Pet` 的根因是 MenuBarExtra 内容未订阅 AppDelegate 的状态变化。最终使用 `.canJoinAllSpaces` 与 `MochiDockMenuContent @ObservedObject` 修复，仍以真实 `NSPanel.isVisible` 为唯一可见性事实来源。
- `.canJoinAllSpaces` 扩展了阶段 1A 最初未启用的跨 Space 行为；用户在看到最终实现和验证结果后授权提交，本次将其作为 MD-003 启动可见性修正接受，后续仍可在人工体验中调整。

## Skill 设计恢复核对

- 当前仓库没有原始对话导出文件，先前隐藏项目目录中的设计文件也不在当前磁盘视图。
- `task_plan.md`、`findings.md` 和 `progress.md` 已包含先前讨论的关键结论，足以继续设计而无需重新讨论。
- AI 开发模式总结已独立整理到 `sources/ai-development-mode-summary.md`。
- `mochidock-pm` 第一版蓝图已写入 `skill-blueprints/mochidock-pm.md`。
- 用户确认把 Skill 放在集中式仓库 `/Users/tao/Projects/Working/skills/mochidock-pm/`，并通过 `~/.agents/skills/mochidock-pm` 发现。
- 官方快速校验通过；独立前向测试确认 Skill 能控制范围、保留用户决策权并阻止未经确认的立即实施。

## MD-004 工程实现结论

- 阶段 1B 动画基础采用单一可取消调度任务：模型拥有待机、吸气、呼气、短促回应和恢复状态；面板只在 Show/Hide 时启动或停止，重复 Show 与尺寸切换不会新增循环。
- 呼吸参数集中在 `PetAnimationTiming.standard`：纵向峰值 `1.022`、吸气 `1.35s`、呼气 `1.55s`、静止间隔 `3.8s`；横向比例固定 `1.0`，SwiftUI 缩放锚点为 `.bottom`。
- 点击沿用现有轻微放大、上移和亮度反馈，但改为单次有界回应后自动恢复；回应中连续点击被忽略，不形成动作队列。
- Hide 会取消当前待执行任务并复位动画几何状态；为保留既有交互契约，当前 mood 不会在 Hide 瞬间被清除，Show 后若为 happy 会恢复一个有界回应并自动回待机。
- v0.4 三档应用资源与正式预览源文件逐对 SHA-256 一致；运行时测试验证 80/120/160 像素和 Alpha。
- 用户在 16 英寸 MBP 上体验后认为 160 仍偏小，确认在 MD-004 内新增 240/320 两档；默认 120 不变，动画、面板与菜单机制不变。
- 用户要求为应用补齐 1024×1024 App Icon，并将 Asset Catalog 从平铺列表改为可扩展分类。图标采用与 v0.4 一致的趴姿小熊猫头像、暖奶油背景；最终源图为 1024×1024 不透明 PNG，无文字、无额外物件。
- Asset Catalog 分类确认为 `System/{AppIcon, AccentColor}` 与 `Characters/RedPanda/Prone/v0.4/{80,120,160,240,320}`；目录不启用 namespace，因此保持现有运行时资源名。

## 本地持久化已确认决策

- 用户确认采用分层存储：UserDefaults 保存显示尺寸、音效和动画等轻量偏好；SwiftData 保存未来成长值、等级、已拥有服饰、穿戴关系和互动统计等领域数据。
- 角色、服饰与动画图片不存入数据库，继续使用 Asset Catalog 或后续资源包；数据库只保存稳定资源标识。
- 第一个可验证步骤只保存并恢复尺寸，为未来偏好扩展建立可测试边界；在成长或服饰真实字段确认前，不为空模型提前创建 SwiftData 实体。
- 该决策形成 `MD-006`，建议在 `MD-005` 前实施；任务当前为“待用户安排”，未自动启动开发。

## MD-006 工程实现结论

- 尺寸偏好通过 `PetPreferencesStoring` 注入边界保存，生产实现封装 `UserDefaults.standard`，测试全部使用每例独立内存存储或唯一 suite，不依赖开发机真实偏好。
- 稳定迁移契约为键 `pet.displaySize` 与 `PetDisplaySize.rawValue`（`small`、`medium`、`large`、`extraLarge`、`jumbo`）；缺失、空值、point 数字、展示文案或旧标识均回退到 120。
- 恢复在 `PetInteractionModel` 同步初始化中完成；AppDelegate 先创建模型，再把同一模型交给面板，因此首个 frame、`PetView` 资源和 Picker selection 不存在第二份尺寸状态。
- 实际生产偏好设为 `jumbo` 后，两次启动干净构建产物均直接显示 320 尺寸角色；Computer Use 无法打开 MenuBarExtra Picker，菜单勾选和启动瞬间无跳变仍需人工目视验收。
- 独立代码审查未发现 Critical 或产品实现缺陷；指出的菜单选择源与 UserDefaults 适配器测试缺口已补齐。
- 用户已完成尺寸菜单、切换、退出重启、启动表现及重复 Hide/Show 的人工验证并确认没有问题；MD-006 于 2026-07-20 验收完成。

## 本地化已确认决策

- 用户确认 MochiDock 现在建立本地化基础，首批语言为英文与简体中文；英文作为开发和回退语言。
- 本地化使用 Apple 原生 String Catalog，并跟随 macOS 系统或应用语言设置；首轮不增加应用内语言切换器或语言偏好持久化。
- 首轮覆盖菜单、五档尺寸展示名称及无障碍文案；`MochiDock` 品牌名保持不翻译。
- 本地化展示文本不得改变 `pet.displaySize`、五个 `PetDisplaySize.rawValue`、资源名称或其他稳定程序标识。
- 德语、繁体中文、日语和其他语言留在想法池，根据目标用户与发布计划另行确认。
- 该决策形成 `MD-007`，建议顺序为：完成 MD-006 人工验收，实施 MD-007，再继续 MD-005。
