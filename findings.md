# MochiDock 已确认结论

## 产品定位

- 首个产品形态是 macOS 桌面宠物。
- 视觉和交互灵感包括早期杀毒软件桌面助手与 QQ 宠物。
- 后续希望逐步加入工具能力。
- 对 AI 有兴趣，但当前尚未确定适合的 AI 功能。
- 产品方向允许在开发和实际使用过程中逐步形成。

## 第一阶段验证重点

- 桌面显示是否稳定。
- 拖动、点击和鼠标交互是否自然。
- 动画和状态切换是否舒适。
- 是否干扰正常工作。
- 菜单、设置和退出是否清晰。
- 用户是否愿意长期保留桌面宠物。

## 项目管理原则

- 用户保留项目经理、产品负责人和最终决策者角色。
- 项目采用轻量范围管理，不引入不必要的企业流程。
- 使用“当前正在做 / 下一步可能做 / 想法池”管理优先级。
- 当前高级项目经理 Agent 每个阶段结束时汇总：已完成、当前进行、风险、阻塞、下一步和待确认事项。
- 当前高级项目经理 Agent 不直接编写产品代码；Codex 开发部门负责开发实现和技术自测。
- 需要开发时，由高级项目经理 Agent 说明应安排的开发任务并提供交付说明，用户负责决定和安排 Codex 开发任务。
- 高级项目经理 Agent 可通过只读检查进行验收，不替代开发部门实施。
- 关键产品或高风险决策仍由用户确认。

## 代码质量原则

- 500–600 行是文件规模警戒范围，而不是机械限制。
- 低耦合、单一职责和可在有限上下文中完整理解比单纯行数更重要。
- 入口与实现分离是合理方向。
- 不应为追求小文件而机械拆分，也不应建立万能工具类。

## AI 协作原则

- 不把聊天上下文当作长期项目记忆。
- 不按固定 token 百分比机械换 Agent，而是观察上下文质量是否退化。
- Handoff 记录某次任务的现场；Skill 记录同类任务以后应如何执行。
- 交接文件必须区分已确认事实、未验证推测、建议和待用户决定事项。
- 新 Agent 接管后先只读核对，不直接继续修改。
- Skill 应少而清晰、按需加载，并像代码一样接受检查和维护。

## MochiDock PM Skill 边界

- 它是 Codex 的项目管理辅助工作流，不替代用户的 PM 身份。
- 第一版应聚焦需求整理、小阶段规划、范围控制、进度与风险汇总、Handoff 和接管验证。
- 暂不建立大量角色型 Skill。
- 新增或重大修改 Skill 前，应由用户确认其是否真正通用。

## 输出体验要求

- 完整 Markdown、Codex 提示词、配置文件或较长代码应保持为单一连续内容。
- 对话界面本身的复制问题无法由 Skill 修复，但可以通过输出结构降低内容断开的概率。
- 长内容优先落盘并提供文件链接。

## 待确认事项

- 小熊猫第一轮视觉草图完成后，从候选轮廓与面部花纹中选择最终方案。
- 阶段 1 的具体交互原型和验收标准。
- 第一个值得尝试的工具能力与 AI 场景。

## 阶段 1B 已确认方向

### MD-005 第二轮运行时闪烁诊断

- 仓库内没有其他 `transition`、`contentTransition`、`opacity` 或 AppKit frame 动画；`NSHostingView` 只在首次创建面板时建立，点击、眨眼、恢复待机和尺寸切换均不重建根视图或 `NSPanel`。
- `PetView` 唯一的 `.animation(value: animationState)` 位于动态 `Image`、`resizable`、`scaledToFit`、`scaleEffect` 与 `offset` 整条修饰链之外，因此 animationState 变化所建立的隐式动画事务会向包含资源替换的整个子树传播；当前没有资源叶节点的禁动画事务边界。
- `handleClick`、三段眨眼和 `returnToIdleAndScheduleNextAction` 都在一次主线程调用中依次修改 `visualState` 与 `animationState`，所以开心切入、三个眨眼节点及恢复待机都会与几何动画状态共享同一轮 SwiftUI 更新。该结构与所有离散切图均透底、素材 Alpha 已稳定的现场现象一致。
- 尺寸切换当前先发布 `model.displaySize`，SwiftUI 可立即选择新资源和 frame，随后控制器才调用 `panel.setFrame(... animate: false)`；虽然 AppKit frame 本身明确不动画，但模型内容与透明面板缓冲区之间没有禁止屏幕中间更新的提交边界。
- 最终根因确认分为两个运行时边界：动态 `Image` 资源和连续几何属性共享同一隐式动画事务；尺寸切换又以 `display: true` 先后发布内容与窗口变化。素材像素不变量已通过且边缘现场稳定，因此本轮没有再修改素材。
- 修正后资源叶节点和 displaySize 更新分别使用 `animation=nil`、`disablesAnimations=true` 的 SwiftUI transaction；只有呼吸、压缩、弹起、落下和恢复返回非空 easeInOut 几何动画规格，三段眨眼及 idle 不建立几何动画。
- 尺寸选择在同一禁动画 SwiftUI transaction 与 `NSAnimationContext(duration: 0, allowsImplicitAnimation: false)` 中更新模型和面板；面板先 `setFrame(display: false, animate: false)`，布局完成后只 `displayIfNeeded()` 一次，避免展示中间缓冲区。`NSPanel`、model、`NSHostingView` 和中心位置保持不变。

- 用户确认阶段 1B 继续聚焦最基础的桌面宠物体验，不实现想法池中的自动走动、飞行、工具、AI、换装或成长系统。
- 用户确认将待机姿势从当前站立改为趴着；站立形象仍可作为已有角色识别基线，但不作为阶段 1B 的常驻待机状态。
- 阶段 1B 继续使用 120 px 作为默认显示尺寸，同时保留 80/120/160 三档切换供体验验证。
- 首轮待机生命感为趴姿下的轻微呼吸或身体起伏、偶尔眨眼，动作之间需要有停顿，不持续晃动。
- 首轮点击回应采用“开心弹起后自动恢复趴姿待机”的单一可验证动作。
- 鼠标响应仅作为阶段 1B 之后的可能下一步，不进入当前承诺范围。
- 用户已确认趴姿静态候选 v0.4 的姿势、与 v0.3 的角色一致性、尾巴位置和颜色；未发现需要调整的造型差异。
- 用户已确认纯本地色键与局部去绿方案 v5 的透明边缘结果；该结果已原样升格为 v0.4 趴姿正式母版，并从其确定性生成 80/120/160 px 三档透明素材。
- v0.4 素材处理期间不使用付费图片 API；角色内部像素不重新绘制，正式素材只从用户确认的本地处理结果晋升。
- 生成式编辑不适合当前的轻微呼吸帧：首个候选改变了绿幕颜色，却只产生约 1 像素的主体边界变化，120 px 下动作不可见。当前推荐验证复用同一透明图的确定性纵向缩放，以避免纹理漂移和背景闪烁；是否正式改用该实现仍待用户确认。
- 用户已确认可看到确定性透明预览的起伏，同意先试用该版本。阶段 1B 呼吸正式改为复用同一透明趴姿素材、底部锚定的程序化纵向缩放；不再制作或接入呼吸图片帧。
- 用户确认阶段 1B 后续采用“三张新母帧＋程序化弹起”方案：新母帧只承担必要的离散造型/表情变化，点击弹起的位移、缩放、节奏、回落与恢复待机由程序实现，不制作完整逐帧弹跳序列。三张母帧指三张高分辨率动作源图，不是 80/120/160 等显示尺寸副本；逐张画面定义需在素材制作前单独锁定。
- 用户已锁定三张新母帧的逐张定义：半闭眼趴姿、全闭眼趴姿、开心趴姿。眨眼按“原待机→半闭眼→全闭眼→半闭眼→原待机”播放；点击时使用开心趴姿，并由程序完成压缩蓄力、向上弹起、落下和恢复原待机。
- 用户确认三张 v1 候选的视觉预览暂时没有问题。三张透明候选已原样升格为 v0.4 正式动作母帧，正式文件与已确认候选 SHA-256 分别一致；每张母帧均确定性生成 80/120/160/240/320 五档透明素材。实际桌面接入后仍保留节奏和细微视觉调整空间。

## 小熊猫角色母版 v0.2

- 用户已确认首版角色为圆润幼崽体型的小熊猫，性格活泼、好奇、略冒失。
- 用户已确认尾巴承担飞行功能，飞行风格为柔软、略笨拙的喜剧飞行。
- 用户已确认主毛色为稍明亮的自然红棕，眼睛为中等大小圆眼。
- 成长型外观、服装、配件和其他性格分支属于未承诺创意，不进入首版范围。
- 完整比例、固定识别点、飞行语言和禁止项见 [角色母版 v0.2](docs/design/character/character_master_v0.2.md)。

## 阶段 1A 工程核对

- 当前 Xcode 为 26.4.1，项目的 macOS 部署目标为 26.4，和本机工具链一致。
- 当前应用仍是 SwiftData 模板：`MochiDockApp.swift` 创建 `ModelContainer`，`ContentView.swift` 展示项目列表，`Item.swift` 是时间戳模型；这些都不属于桌面宠物职责。
- Xcode 工程使用文件系统同步分组，新建 Swift 文件会自动纳入对应 target，不需要手工编辑 `project.pbxproj`。
- 阶段 1A 采用 AppKit `NSPanel` 管理透明浮动窗口，SwiftUI 只负责绘制和交互状态，避免让入口文件同时承担窗口与视图职责。
- 阶段 1A 不引入持久化；是否保存宠物位置和设置留给后续阶段，由用户确认后再决定。
- `xcodebuild -list` 能识别 `MochiDock` scheme；受当前沙箱限制，命令输出包含 CoreSimulator 与日志权限警告，但 macOS 项目和 scheme 仍被成功读取。正式构建验证将在实施阶段执行。

## 阶段 1A 开发初审发现

- 产品实现采用长期持有的单一 `NSPanel`、独立互动状态模型和 SwiftUI 系统绘图，占位形象与窗口生命周期职责分离，符合任务书边界。
- 用户已确认实际启动、单一透明无边框显示，以及第一次点击后变为开心；第二次点击恢复、实际拖动、菜单恢复与退出、低打扰表现和 30 分钟稳定性仍需人工验收。
- 自动测试验证了状态往返、窗口透明/无边框/可拖动配置、重复显示复用和关闭后恢复；测试属性检查不能替代真实拖动与点击手势组合的人工验证。
- 面板测试会展示真实 `NSPanel` 但未主动清理，且缺少 `hidePet()` 后恢复的专门回归测试；两项均为非阻塞质量改进，不影响进入人工验收。

## MD-002 工程核对

- 正式角色三档资源使用独立 image set，未错误映射成同一资源的 1x/2x/3x；当前 scale 1 方案只服务体验评估，不代表最终 Retina 素材体系。
- 默认尺寸为 120；尺寸状态、资源名和 point 边长集中在 `PetDisplaySize`，面板控制器切换同一 `NSPanel` 的 frame 并保持中心。
- 高级项目经理独立复跑 22 个测试和干净 Debug 构建均成功，资源像素、alpha、哈希和构建产物 rendition 均与 Handoff 一致。
- 未发现 Critical 或 Important 代码问题。测试未主动清理真实面板是非阻塞质量项；全视图点击手势与 `isMovableByWindowBackground` 的组合必须通过真实拖动验收。
- 人工验收确认尺寸切换和 30 分钟稳定性通过。
- `PetPanelController.hidePet()` 已存在，但没有从 `MochiDockAppDelegate` 和菜单暴露；无边框面板又没有关闭按钮，导致用户无法进入隐藏状态。当前 `Show Pet` 在宠物已显示时无视觉变化属于幂等行为，真正缺口是缺少可达的 `Hide Pet` 操作。
- MD-003 首轮实现同时显示 `Show Pet` 与 `Hide Pet`。用户要求合并为单一动态菜单项：面板可见时提供 `Hide Pet`，面板隐藏时提供 `Show Pet`；不使用固定的 `Toggle Pet` 文案。
- MD-003 第二次开发报告复用了旧的 `hidePet` RED 和 27 项测试，但没有修改两个独立菜单按钮，也没有新增动态标题、单一切换入口或 Handoff 修订记录；旧测试通过不能证明新交互需求已实现。
- MD-003 最终修订使用单一动态按钮；标题直接读取 `PetPanelController.isPetVisible`（实际 `NSPanel.isVisible`），AppDelegate 的 `ObservableObject` 通知仅触发 SwiftUI 重新求值，没有保存第二份可见状态。独立复核确认固定双按钮已移除，30 个测试和干净构建通过。
- 用户一度建议固定 `Show/Hide Pet`，进一步排查后最终保留更明确的动态标题：可见时 `Hide Pet`，隐藏时 `Show Pet`。
- 启动后宠物看似未显示的根因是面板可能停留在其他 Space；菜单一直显示 `Show Pet` 的根因是 MenuBarExtra 内容未订阅 AppDelegate 的状态变化。最终使用 `.canJoinAllSpaces` 与 `MochiDockMenuContent @ObservedObject` 修复，仍以真实 `NSPanel.isVisible` 为唯一可见性事实来源。
- `.canJoinAllSpaces` 扩展了阶段 1A 最初未启用的跨 Space 行为；用户在看到最终实现和验证结果后授权提交，本次将其作为 MD-003 启动可见性修正接受，后续仍可在人工体验中调整。

## Skill 设计恢复核对

- 当前仓库没有原始对话导出文件，先前隐藏项目目录中的设计文件也不在当前磁盘视图。
- `task_plan.md`、`findings.md` 和 `progress.md` 已包含先前讨论的关键结论，足以继续设计而无需重新讨论。
- AI 开发模式总结已独立整理到 `sources/ai-development-mode-summary.md`。
- `mochidock-pm` 第一版蓝图已写入 `skill-blueprints/mochidock-pm.md`。
- 用户确认把 Skill 放在集中式仓库 `/Users/tao/Projects/Working/skills/mochidock-pm/`，并通过 `~/.agents/skills/mochidock-pm` 发现。
- 官方快速校验通过；独立前向测试确认 Skill 能控制范围、保留用户决策权并阻止未经确认的立即实施。

## MD-004 工程实现结论

- 阶段 1B 动画基础采用单一可取消调度任务：模型拥有待机、吸气、呼气、短促回应和恢复状态；面板只在 Show/Hide 时启动或停止，重复 Show 与尺寸切换不会新增循环。
- 呼吸参数集中在 `PetAnimationTiming.standard`：纵向峰值 `1.022`、吸气 `1.35s`、呼气 `1.55s`、静止间隔 `3.8s`；横向比例固定 `1.0`，SwiftUI 缩放锚点为 `.bottom`。
- 点击沿用现有轻微放大、上移和亮度反馈，但改为单次有界回应后自动恢复；回应中连续点击被忽略，不形成动作队列。
- Hide 会取消当前待执行任务并复位动画几何状态；为保留既有交互契约，当前 mood 不会在 Hide 瞬间被清除，Show 后若为 happy 会恢复一个有界回应并自动回待机。
- v0.4 三档应用资源与正式预览源文件逐对 SHA-256 一致；运行时测试验证 80/120/160 像素和 Alpha。
- 用户在 16 英寸 MBP 上体验后认为 160 仍偏小，确认在 MD-004 内新增 240/320 两档；默认 120 不变，动画、面板与菜单机制不变。
- 用户要求为应用补齐 1024×1024 App Icon，并将 Asset Catalog 从平铺列表改为可扩展分类。图标采用与 v0.4 一致的趴姿小熊猫头像、暖奶油背景；最终源图为 1024×1024 不透明 PNG，无文字、无额外物件。
- Asset Catalog 分类确认为 `System/{AppIcon, AccentColor}` 与 `Characters/RedPanda/Prone/v0.4/{80,120,160,240,320}`；目录不启用 namespace，因此保持现有运行时资源名。
- 用户已完成人工验收并于 2026-07-20 再次明确确认 `MD-004` 已验收。高级项目经理收口复核确认实现、资源结构和任务要求一致；指定全部有效 `MochiDockTests` 新鲜测试与独立干净 Debug 构建均成功。完整 scheme 中空 `MochiDockUITests` bundle 在无签名环境下缺少可执行文件，作为非阻塞测试配置问题保留。
- `MD-004` 已完成并归档；用户于同日正式安排 `MD-005`，开发范围保持为偶尔眨眼与单次程序化开心弹起。

## MD-005 工程实现结论

- 用户在实际点击验收中观察到开心帧切换闪烁。代码核对确认单一面板未重建，点击同时发生整图资源切换、亮度增加和程序化压缩。
- 120 px 像素诊断显示待机与开心帧 Alpha 包围盒同为 `(15, 26, 116, 92)`，但 809 个像素的 Alpha 值不同；共同完全不透明区域中 3330 个像素 RGB 变化。变化不只位于眼睛和嘴部，还分布于耳朵、身体和底部抗锯齿边缘。
- 根因是动作图独立生成并独立去背，导致角色静止区域和 Alpha 蒙版无法逐像素稳定；透明窗口使这些帧差表现为闪烁。亮度增益 `0.025` 会进一步放大切换感，但单独移除亮度不能解决素材边缘和主体变化。
- 推荐从素材源头修复：以原待机透明帧作为固定基底，仅合成必要的表情区域，让非表情像素和整张 Alpha 轮廓保持一致；不以淡入淡出掩盖漂移。
- 闪烁修正启动时重新目视核对 1254×1254 母帧：半闭眼与全闭眼的必要变化局限于双眼，开心帧的必要变化局限于双眼和嘴部；耳朵、眉毛、脸部毛发、身体、四肢与尾巴都应直接保留原待机基底。原始 v1 workbench 候选继续保留作对比，不作为稳定像素基底。
- 稳定素材使用版本化矩形许可区：左眼 `(315,515)–(425,660)`、右眼 `(475,515)–(590,660)`、嘴部 `(365,640)–(545,755)`；实际合成使用许可区内圆角蒙版与 7 px 高分辨率羽化，羽化结果再次裁回许可区。半闭眼/全闭眼只启用双眼，开心启用双眼和嘴部。
- 三张稳定母帧最终整张 Alpha 直接取自原待机母版；许可区外 RGBA 逐像素等同待机。五档图以对应既有待机 Catalog 图作为尺寸基底，只贴入稳定母帧缩放后的许可区，并强制使用该档待机 Alpha，因此 80/120/160/240/320 的运行时切换不改变外轮廓或非表情像素。
- 本轮没有重新生成角色或去背，也未覆盖原始 v1 workbench 候选；确定性脚本、蒙版 JSON、像素测试共同构成可复现的素材验证边界。

- 四种视觉状态使用稳定资源映射：`idle` 保持 `RedPandaProneV04_<px>`，新增 `RedPandaProneHalfBlinkV04_<px>`、`RedPandaProneFullBlinkV04_<px>` 与 `RedPandaProneHappyV04_<px>`；每种状态覆盖 80/120/160/240/320 五档。
- 现有单一可取消调度任务扩展为有限状态序列。每完成 3 次带 `3.8s` 停顿的呼吸后播放一次 `0.09s`/帧的半闭、全闭、半闭眨眼，再恢复原待机并重新停顿。
- 点击优先于眨眼和呼吸：点击先取消当前调度，切换开心母帧，并依次执行 `0.10s` 压缩蓄力、`0.16s` 上升、`0.18s` 落下、`0.12s` 恢复；连续点击在这四个阶段均被忽略，不形成队列。
- Hide 会取消当前动作、恢复原待机资源和几何状态；Show 与重复 Show 只恢复一个待机调度。尺寸切换继续复用同一面板和同一模型。
- 本任务没有修改偏好键、本地化目录、菜单结构、签名、Bundle Identifier、deployment target 或依赖。

## 本地持久化已确认决策

- 用户确认采用分层存储：UserDefaults 保存显示尺寸、音效和动画等轻量偏好；SwiftData 保存未来成长值、等级、已拥有服饰、穿戴关系和互动统计等领域数据。
- 角色、服饰与动画图片不存入数据库，继续使用 Asset Catalog 或后续资源包；数据库只保存稳定资源标识。
- 第一个可验证步骤只保存并恢复尺寸，为未来偏好扩展建立可测试边界；在成长或服饰真实字段确认前，不为空模型提前创建 SwiftData 实体。
- 该决策形成 `MD-006`，建议在 `MD-005` 前实施；任务当前为“待用户安排”，未自动启动开发。

## MD-006 工程实现结论

- 尺寸偏好通过 `PetPreferencesStoring` 注入边界保存，生产实现封装 `UserDefaults.standard`，测试全部使用每例独立内存存储或唯一 suite，不依赖开发机真实偏好。
- 稳定迁移契约为键 `pet.displaySize` 与 `PetDisplaySize.rawValue`（`small`、`medium`、`large`、`extraLarge`、`jumbo`）；缺失、空值、point 数字、展示文案或旧标识均回退到 120。
- 恢复在 `PetInteractionModel` 同步初始化中完成；AppDelegate 先创建模型，再把同一模型交给面板，因此首个 frame、`PetView` 资源和 Picker selection 不存在第二份尺寸状态。
- 实际生产偏好设为 `jumbo` 后，两次启动干净构建产物均直接显示 320 尺寸角色；Computer Use 无法打开 MenuBarExtra Picker，菜单勾选和启动瞬间无跳变仍需人工目视验收。
- 独立代码审查未发现 Critical 或产品实现缺陷；指出的菜单选择源与 UserDefaults 适配器测试缺口已补齐。
- 用户已完成尺寸菜单、切换、退出重启、启动表现及重复 Hide/Show 的人工验证并确认没有问题；MD-006 于 2026-07-20 验收完成。

## 本地化已确认决策

- 用户确认 MochiDock 现在建立本地化基础，首批语言为英文与简体中文；英文作为开发和回退语言。
- 本地化使用 Apple 原生 String Catalog，并跟随 macOS 系统或应用语言设置；首轮不增加应用内语言切换器或语言偏好持久化。
- 首轮覆盖菜单、五档尺寸展示名称及无障碍文案；`MochiDock` 品牌名保持不翻译。
- 本地化展示文本不得改变 `pet.displaySize`、五个 `PetDisplaySize.rawValue`、资源名称或其他稳定程序标识。
- 德语、繁体中文、日语和其他语言留在想法池，根据目标用户与发布计划另行确认。
- 该决策形成 `MD-007`，建议顺序为：完成 MD-006 人工验收，实施 MD-007，再继续 MD-005。
- MD-007 启动盘点确认：`developmentRegion` 已为 `en`，工程使用文件系统同步分组且开启 String Catalog 支持；当前没有 `.xcstrings`，`knownRegions` 仅有 `en` 与 `Base`。
- 当前运行时用户可见文案集中在动态 `Show Pet` / `Hide Pet`、`Pet Size`、`Quit MochiDock`、五档尺寸展示名、`MochiDock pet` 无障碍标签及 `Resting` / `Happy` 状态值；Preview 名称不在本地化范围。
- 展示名称与稳定契约已分离：`pet.displaySize`、五个 `PetDisplaySize.rawValue` 与五个角色资源名均无需因本地化修改。
- MD-007 最终实现使用一个 `Localizable.xcstrings` 提供 12 个英文源键与简体中文翻译；构建产物的 `Bundle.localizations` 为 `en`、`zh-Hans`，且包含对应两份编译后 `Localizable.strings`。
- 动态显示/隐藏标题仍直接读取 `PetPanelController.isPetVisible`；尺寸展示只把 `menuTitle` 改为 `LocalizedStringResource`，偏好键、raw value、点数、资源名和保存时机未改变。
- 实际双语启动的无障碍树分别显示 `MochiDock pet` / `Resting` 与 `MochiDock 宠物` / `休息中`；当前 Computer Use 不能访问 MenuBarExtra，菜单目视与短暂 Happy 状态仍需人工验收。
- 用户完成 MD-007 实际检查并确认没有问题；独立提交前复核未发现 Critical、Important 或 Minor 代码问题，MD-007 于 2026-07-20 验收完成。
- 高级项目经理独立复核运行了新鲜素材测试、全部有效应用单元测试及干净 Debug 构建，结果均通过；三张 stable-v2 母帧静态查看未见明显接缝。由此可确认工程与像素不变量已达到人工复验门槛，但不能替代真实桌面上的动态观感判断。
- 用户动态复验证明像素稳定并不足以保证透明窗口切图无闪烁：当前 `.animation(value: animationState)` 覆盖整条 `Image` 修饰链，而图片状态与动画状态同步变化，是资源替换被纳入隐式动画事务的高可信风险点；尺寸切换则同时改变资源、SwiftUI frame 与 `NSPanel` frame，缺少明确的原子重绘边界。两者需由下一轮开发先诊断证明，不能把静态推断直接当作最终根因。
- 第二轮运行时修正将离散资源切换与连续几何动画分离，并将尺寸模型与透明面板更新置于禁用隐式动画的更新边界；独立自动复核全部通过，用户真实桌面初步复验也反馈基本无问题。该组合证据已达到正式验收门槛，但仍需用户明确作出最终验收决定。
- 用户随后认可阶段 1 收口规划并确认 MD-005 已跑通。MD-001、MD-002、MD-003 的早期人工验收缺口均已被当前版本的长期使用、后续自动回归和真实桌面检查覆盖，因此与 MD-005 一并正式完成归档，阶段 1 结束。
- 阶段 2 首个推荐探索是二元的鼠标接近回应，而不是持续鼠标追踪：只感知进入/离开附近区域，以一张注意姿态母帧配合轻微程序化抬起为推荐方案；左右看向、追逐鼠标和复杂行为系统均不进入首轮范围。
- 用户补充确认尾巴应成为 MD-008 的明显反馈通道，因为小熊猫尾巴比细小面部变化更容易辨认。首轮建议因此调整为注意基础帧与尾巴强调帧两张母帧：只播放一次短促尾巴回应，不持续循环摇尾；身体和非许可区域继续保持稳定。
- MD-008 首轮组合已确认：轻微抬头加耳朵注意＋尾巴尖轻抬＋短暂回应后自行恢复待机。鼠标持续停留不循环触发，必须离开并经过冷却才能再次回应；下一步只制作两张视觉候选，不进入代码开发。
- MD-008 尾巴候选验证表明，只约束“外段抬起”可能导致生成图在尾根处出现断开。后续视觉与正式素材验收必须把“臀部至尾尖形成连续、自然厚度的单一轮廓”列为先于幅度和辨识度的硬性条件；断尾 v3 已明确淘汰。
- MD-008 正式素材不能沿用 MD-005 的整图 Alpha 固定策略，因为尾巴真实移动必然改变局部轮廓。新的稳定契约是：头部与尾巴版本化许可区内允许必要变化，许可区外 RGBA/Alpha 严格不变；尾根锚点在两张注意帧间保持连续和稳定，并由自动测试与多背景目视共同验证。
- 用户重申 MD-008 不使用付费图片 API；沿用此前已确认的 ChatGPT/Codex 内置 GPT 图像能力加本地后处理路线。任何需要 `OPENAI_API_KEY` 并向外部 Image API 上传项目素材的方案均不进入当前执行范围。
- 尾巴等前景部件移动时，如果原部件遮挡了身体，问题不只是“清除旧轮廓”：被遮挡的身体像素在原图中根本不存在，必须在独立许可区内重建身体，再与新部件统一合成。继续缩裁尾巴蒙版只会在“残留旧尾巴”和“后背/后腿缺失”之间来回切换。
- MD-008 最终正式素材采用 v3 `tailMotion + bodyReconstruction` 联合许可区和缩小后的实体臀部硬锚点。用户多背景验收、确定性哈希、11 项素材测试及干净 Debug 构建共同通过；视觉素材阶段已完成，代码阶段仍需用户正式安排。
- 用户已正式安排 MD-008 代码阶段。现有 `PetInteractionModel` 已集中持有单一可取消调度任务，`PetPanelController` 持有单一面板，`PetRenderPolicy` 隔离离散资源切换；因此实现应在这些边界上增加可测试的接近检测职责和有限状态，而不是建立第二条动画循环或把平台检测堆入 `PetView`。
- MD-008 实现验证了无需权限的有限接近路径：只在面板可见时以约 6.67 Hz 读取 `NSEvent.mouseLocation`，不安装事件监视器；面板自身 `sendEvent` 只桥接左键按下/抬起以暂停和重建接近状态。独立技术复核未发现阻止人工验收的问题，但真实拖动、边缘手感和长期能耗仍不能由单元测试替代。
- 用户已完成 MD-008 真实桌面验收并明确确认通过。有限接近检测、单次注意与尾巴回应、4 秒冷却、点击/拖动优先级及 Hide/Show 行为成为当前阶段 2 已验收基线；后续问题按新的明确修订范围处理。

## 阶段 2 后续规划提案

- 已验收代码具备拖动能力和轻量 `UserDefaults` 偏好边界，但仍未保存面板位置；应用创建面板时固定调用 `center()`。因此“退出重启后保持用户摆放位置”是当前最直接、范围最小的日常使用改进。
- 推荐的 MD-009 是位置保存与安全恢复：只在明确位置提交点保存，并在当前屏幕 `visibleFrame` 中校正，覆盖负坐标、多屏变化、尺寸变化和无效数据回退。
- 位置持久化属于需要用户确认的数据保存范围；最初规划时只形成提案，随后已由用户明确授权并建立 MD-009 任务书。
- 后续候选按真实痛点选择低打扰控制、登录启动或阶段 2 收口试用；设置页、成长系统、工具、AI 和发布继续留在未承诺范围。
- 用户提出未来的屏幕边缘躲藏行为：宠物可以主动藏到屏幕外，只露出脑袋偷偷看用户。该想法已明确放入未承诺想法池，暂不实施，也不并入 MD-009。
- 位置安全恢复与主动躲藏必须保持不同语义：前者防止屏幕配置变化或无效数据造成窗口丢失；后者若未来实施，应由显式行为状态授权部分越界，并保证用户能发现、唤回且不会误判为应用故障。
- 用户已明确同意在本机保存宠物窗口位置，MD-009 的数据保存决定因此获得授权。普通保存位置恢复时必须完整位于当前屏幕 `visibleFrame` 内；未来主动躲藏不作为本任务例外。
- MD-009 任务边界已确定：拖动结束保存、启动前恢复、无效数据回退、五档尺寸安全校正、运行中屏幕参数变化复核和 Hide/Show 位置不变量；不保存轨迹、动画状态、Space 或隐藏状态。
- MD-009 首轮独立复核发现 Important 通知顺序缺口：如果 AppKit 在 `didChangeScreenParametersNotification` 送达前已把窗口移动到安全位置，处理器会看到当前 frame 无需校正而跳过写入，导致 `pet.windowPosition` 仍保存旧屏幕坐标。任务要求运行中屏幕变化后的实际安全位置同步保存，因此必须比较当前 origin 与持久化 origin，并增加对应顺序的回归测试。
- MD-009 第一项 Important 已由开发部门修正：有效旧保存坐标与 AppKit 预移动后的安全 origin 不同时会同步保存，且不重复移动 frame。第二轮复审发现 `persistedOrigin == nil` 同时代表缺失值与无效值，当前比较会使未变化的屏幕通知创建新位置值，违反“无需移动不写入”。应只在存在有效持久化 origin 且不一致时触发该同步分支。
- MD-009 第二项 Important 已修正。第三轮复审发现：当 `visibleFrames` 为空且已有 `pendingRestorationFrame` 时，尺寸切换会清空 pending 并保存未经校正的居中 frame，导致原恢复意图丢失；必须保留或按新尺寸重建 pending，等待屏幕信息恢复后再提交。
- 新增缺失/无效位置零写入测试使用 `NSScreen.main!.visibleFrame`，与任务书要求的确定性、注入式屏幕测试相冲突。测试应使用合成 `visibleFrame` 和可控 panel frame，不依赖开发机主屏存在或布局。
- MD-009 第三轮两个缺口已修正，测试中已无 `NSScreen` 引用。最终复审发现 pending 尺寸重建保留 origin 而非中心点；现有离屏贴边测试会让两种算法得到相同 clamp 结果，不能证明中心语义。需使用不触发 clamp 的合成大屏测试 120→320 后 origin 从 `(500,300)` 变为 `(400,200)`。
- 用户截图中的 `swift_willThrow` 是启用 Swift Error Breakpoint 后的调试暂停，不是当前 MD-009 崩溃证据：截图后无新 MochiDock 崩溃报告，干净 Debug 通过正常 LaunchServices 路径启动后进程持续存活；产品源码没有显式 `throw`、`try!`、`fatalError` 或断言终止路径，JSON Codable 只使用 `try?`。
- 2026-07-20 15:49 与 20:44 的既有 `.ips` 均为 AppKit `_RegisterApplication` 阶段 `SIGABRT`，发生在产品逻辑之前，与先前直接/探针式启动环境一致，不指向 MD-009 位置代码。
- 用户已确认正常桌面使用中的位置保存没有问题并授权提交当前实现。该确认覆盖主流程人工体验，但不改变独立复审结论：无屏幕信息的 pending 恢复期间切换尺寸仍需按旧 frame 中心重建；提交记录必须把它保留为已知边界，不能宣称任务书全部条件已满足。
- 用户随后完成更完整检查并明确决定 MD-009 暂时没有问题、可以验收。该产品决定使 MD-009 以当前实现收口；pending/尺寸中心点差异不再作为本任务阻塞项，但仍保留为已接受的低概率边界，不能描述为技术上已修复。
- 用户随后在 Xcode 中继续执行，MochiDock 正常运行，确认本次 `swift_willThrow` 确为 Swift Error Breakpoint 暂停而非产品崩溃。
