# 角色设计进展记录

## 2026-07-19

- 用户确认阶段 1B 的常驻待机应改为趴姿，不再以当前站立图作为待机姿势。
- 首轮动画素材范围确认为：趴姿静态基线、轻微呼吸/起伏、偶尔眨眼，以及开心弹起后恢复趴姿的点击回应。
- 自动走动、飞行、多表情、换装和成长系统继续保留在创意池，不进入本轮制作。
- 已以 v0.3 站姿母版为严格身份参考生成首张趴姿静态候选 v0.4：趴姿、前爪和完整尾巴均清晰，角色整体接近 v0.3，等待用户视觉评审。
- 候选已生成 1254×1254 带 alpha 的 PNG；保守去背保留了完整主体，但细毛外沿仍有轻微绿色污染，因此当前只作为造型候选，不视为生产母版。
- 用户评审确认 v0.4 趴姿良好，与原型 v0.3 暂未观察到造型差异，尾巴位置合适，颜色无异常。下一步只处理透明细毛边缘与小尺寸验证，不改造型。
- 用户已授权使用 `gpt-image-1.5` CLI 原生透明路径处理细毛边缘；命令参数已通过 dry-run 校验，但当前 Codex 终端环境未配置 `OPENAI_API_KEY`，因此真实 API 调用尚未执行。
- 用户后续明确要求不调用任何付费图片 API，改用本地后处理；未上传项目素材，未发生付费 API 调用。
- macOS Vision 前景主体提取只支持 Apple Neural Engine，当前系统无法建立该本地推理计划，且该请求不支持改用 CPU，因此 Vision 路径未产出蒙版。
- 用户授权后，已在项目 `.venv` 中安装本地 `rembg[cpu]` 及 U²-Net 模型，并只生成 `asset/workbench/mochi-v0.4-rembg-test.png` 候选，没有覆盖母版或生成三档尺寸。
- `rembg` 默认输出保留了完整主体，暂未观察到角色内部破洞，但毛发外沿仍有绿色污染；全图 1,572,516 像素中有 397,146 个半透明像素，范围过大。白色/灰色背景诊断图进一步显示边缘光晕与绿边，因此该结果不通过正式素材标准。
- 已改用纯本地确定性混合方案：从源图边界测得色键 RGB (8, 248, 11)，直接按 RGB 色距构建 Alpha，并只在窄边界做颜色延伸和局部去绿。稳定 workbench 候选 v5 保留完整主体，无内部破洞，半透明像素降为 3,602 个，可见绿色占优像素降为 0；仅 3,228 个不透明边界像素发生去色调整，主体内部未修改。
- 用户已确认 workbench v5 边缘观感；该文件原样升格为 `master/v0.4` 正式透明趴姿母版，SHA-256 与已确认候选完全一致，没有再次改动角色像素。
- 已从正式 v0.4 透明母版使用本地 LANCZOS 缩放确定性生成 80×80、120×120、160×160 三档 PNG。三张文件均为 RGBA、带 Alpha 通道且四角完全透明；120 px 预览已完成目视检查。
- 本轮没有调用付费图片 API、没有重新绘制角色，也没有修改或接入产品代码。下一步是基于同一趴姿基线制作轻微呼吸、偶尔眨眼和开心弹起回应帧。
- 已开始下一步的最小验证：使用 v0.4 绿幕母版作为严格编辑参考，通过 ChatGPT 内置图像能力生成一张“呼吸最高点”候选帧 v1；没有调用 `OPENAI_API_KEY` 或付费图片 API。
- 候选仅允许胸背轮廓轻微上抬，头脸、眼睛、四肢、尾巴、花纹、色彩、构图和画布位置均要求保持不变。当前文件只保存在 `asset/workbench/`，并生成 120 px 往返 GIF 供动作幅度评审，尚未透明化或升格正式动画素材。
- 候选帧属于生成式编辑，像素检查显示画面存在全局轻微重算；需先人工确认 120 px 下是否表现为自然呼吸、是否出现毛发闪烁或身份漂移，再决定继续或改用确定性局部变形方案。
- 用户评审确认生成式呼吸候选 v1 不合格：动画中只有背景闪烁，小熊猫没有可见动作。进一步测量显示两帧绿幕基准色明显不同，而主体边界仅约 1 像素变化，在 120 px 下不可读；该候选不透明化、不升格。
- 已建立单一替代假设并生成透明 workbench 预览：完全复用已确认的 120 px 透明素材，以底部为锚点做最高约 2.4% 的纵向起伏。该方案不重绘角色、不改变毛发纹理、不产生背景闪烁，等待用户评审动作幅度。
- 用户确认能够看到该确定性预览的起伏，同意先试用这一版。呼吸方案由“制作新图片帧”正式调整为“复用同一透明素材、底部锚定的程序化纵向缩放”；眨眼与点击回应仍保持独立素材步骤。

- 整理了角色母版 v0.1 的基础方向。
- 用户确认“圆润幼崽”作为首版体型。
- 用户确认“柔软但略笨拙的喜剧飞行”。
- 将“成长型”记录到创意池，明确不进入首版范围。
- 用户确认基础毛色为“稍明亮的自然红棕”。
- 用户确认眼睛为“中等大小的圆眼”。
- 已生成角色母版 V0.2 第一轮 A/B/C 视觉探索稿，并补发去除桌面 Dock 图标的清洁修订版。
- 当前仍未选定最终候选；下一步是评审轮廓与面部花纹，确定单一母版方向，再进入标准视图和小尺寸验证。
- 已确认第一轮图仅是整张概念探索排版稿：文件无透明通道，角色带背景与地面阴影，不能作为可单独使用的产品素材。
- 当前视觉流程的实际阻塞是先选择 A/B/C 中的单一方向，再生成独立、完整、可抠图的基准姿势母版；不建议直接裁切现有排版稿。
- 用户已选定双足探索图中从左到右第二款（中间款）作为基础造型方向，候选选择阻塞已解除。
- 下一步先生成该造型的独立透明基准母版；动画素材将在母版稳定后以透明 PNG 帧序列为首选方案逐项制作。
- 已依据确认的 Prompt 生成中间款独立基准母版候选 v0.3，并保存纯色源图与透明 PNG；当前等待用户评审造型、面部花纹和色彩。
- 普通色键去背保留了完整主体，但细毛外沿仍可能存在轻微绿色残边；该问题不阻塞造型评审，正式产品素材需要在造型确认后再决定是否采用原生透明生成方式。
- 用户确认喜欢独立母版候选 v0.3。推荐下一步先将其作为静态评估素材接入应用，在真实桌面环境验证尺寸、辨识度和遮挡感，再决定待机动画需要哪些帧。
- 已将角色素材按探索稿、版本母版和尺寸预览重组到 `asset/character/red-panda/`，并增加 `asset/README.md` 说明维护规则。
- 已从 v0.3 透明母版确定性生成 80×80、120×120、160×160 三档 PNG，下一步是在接入 Xcode 前评审小尺寸可读性。
