# MochiDock Handoff：MD-001 阶段 1A

## Metadata

- Date: 2026-07-18
- Source task: MD-001 阶段 1A 最小桌面宠物
- Repository path: `/Users/tao/Projects/Working/MochiDock`
- Branch: `main`
- Worktree: 主工作区，存在 MD-001 产品代码及此前项目管理改动
- HEAD commit: `cea1d55 feat: add minimum desktop pet`
- Working tree status: dirty；MD-001 产品代码已提交，项目管理文件尚未提交

## 1. Goal and allowed scope

**Documented agreement:** 将 SwiftData 模板改造成最小可体验 macOS 桌面宠物，包含透明无边框显示、拖动、点击表情反馈、菜单栏恢复与退出，并复用同一个宠物面板。

明确不包含持久化、正式素材、复杂动画、AI、网络、第三方依赖、设置、发布或跨所有 Space/全屏覆盖行为。

## 2. Completed work

**Verified fact:** 已实现约 128×128 的透明无边框浮动宠物面板、SwiftUI 临时 Mochi 形象、点击状态切换、菜单栏 `Show Pet` 与 `Quit MochiDock`、单面板复用；已移除 SwiftData 模板及空模板测试。

**Verified fact:** 用户已实际启动应用，看到单一宠物，并观察到第一次点击后变为笑脸；尚未报告第二次点击恢复休息的人工结果。

## 3. Verified project state

- `project.pbxproj` 没有差异。
- 未发现第三方包或产品源码中的 SwiftData 残留。
- 产品入口、窗口控制、互动状态和视图职责分离；相关产品与测试文件共 249 行，无文件规模风险。
- MD-001 产品代码已提交为 `cea1d55 feat: add minimum desktop pet`；项目文档在本交接更新时尚未提交。

## 4. Confirmed product and business rules

- 用户保留产品经理、产品负责人和最终决策权。
- Stage 1A 只验证最小桌面宠物体验，不扩展工具、AI、持久化或正式视觉方案。
- 高级项目经理只读验收产品代码；开发修改交由 Codex 开发部门执行。

## 5. Technical decisions

- Decision: 一个长期持有、`isReleasedWhenClosed = false` 的 `NSPanel`。
  - Reason: 重复显示与关闭后恢复同一实例，避免窗口增殖。
  - Alternatives excluded: 每次显示创建新窗口。
  - Evidence or constraint: 相关复用测试通过。
- Decision: `PetInteractionModel`、`PetView`、`PetPanelController` 分责。
  - Reason: 状态可测试，视图只绘制与处理点击，控制器只管理面板。
  - Alternatives excluded: 将全部逻辑堆进入口文件。
  - Evidence or constraint: 任务书要求入口聚焦装配与菜单连接。
- Decision: `.moveToActiveSpace`，未启用 `.canJoinAllSpaces` 或 `.fullScreenAuxiliary`。
  - Reason: 显示时出现在活动 Space，同时不跨所有 Space 或覆盖全屏。
  - Alternatives excluded: Stage 1A 擅自启用全空间行为。

## 6. Changed files and interfaces

创建：

- `MochiDock/MochiDock/PetInteractionModel.swift`
- `MochiDock/MochiDock/PetView.swift`
- `MochiDock/MochiDock/PetPanelController.swift`
- `MochiDock/MochiDockTests/PetInteractionModelTests.swift`
- `MochiDock/MochiDockTests/PetPanelControllerTests.swift`

修改：

- `MochiDock/MochiDock/MochiDockApp.swift`

删除：

- `MochiDock/MochiDock/ContentView.swift`
- `MochiDock/MochiDock/Item.swift`
- `MochiDock/MochiDockTests/MochiDockTests.swift`
- `MochiDock/MochiDockUITests/MochiDockUITests.swift`
- `MochiDock/MochiDockUITests/MochiDockUITestsLaunchTests.swift`

## 7. Verification

- **Verified fact:** 高级项目经理于 2026-07-18 重新运行目标测试：退出码 0，6 通过、0 失败。
- **Verified fact:** 高级项目经理重新运行干净 Debug 构建：退出码 0。
- **Verified fact:** `git diff --check` 退出码 0。
- **Verified fact:** 独立只读代码审查未发现 Critical 或 Important 问题，结论为可进入人工验收。
- **Coverage limitation:** 自动测试只能检查可拖动属性，不能证明 SwiftUI 点击手势与 AppKit 背景拖动在真实窗口中同时自然工作。

## 8. Incomplete work, risks, and blockers

- Incomplete work: 实际拖动、菜单恢复/退出、切换应用后的低打扰表现、多次恢复和 30 分钟稳定性尚未完成验收。
- Known risks: 面板测试没有主动关闭或隐藏真实面板；缺少 `hidePet()` 后恢复的专门回归测试。
- Active blockers: 无。
- Unverified assumptions: `.moveToActiveSpace` 在用户实际多 Space/全屏工作方式中的体验仍需观察。

## 9. Decisions still required from the user

当前没有产品方向决定。用户只需决定何时完成剩余人工体验验收；在验收前不应将 MD-001 标记为“已完成”。

## 10. Takeover procedure

1. 只读查看 `git status --short`、本交接和 MD-001 任务书。
2. 检查当前产品代码差异，确认没有夹带后续阶段功能。
3. 重新运行目标测试和干净 Debug 构建。
4. 完成任务书中尚未勾选的人工体验项目并记录准确结果。
5. 只有全部必要验收通过后，才将任务书移入 `docs/tasks/completed/` 并更新项目总览与进度；是否提交 Git 由用户决定。
